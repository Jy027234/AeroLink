# 测试服务器发布预检（2026-09-10）

用户指定了测试服务器连接资料目录。本轮连接指定服务器、准备独立构建目录并完成数据库升级演练，没有切换现有服务、修改现有数据库或推送 GitHub。连接密钥、账号凭据及数据库备份不进入仓库。

## 现有服务与隔离边界

服务器已有 AeroLink，HTTP 80 端口正在使用。网页镜像标签为 `acab499`，API/Worker 为 `823cc2f512f16fd21d95f96b77fbe6146e98d206`，版本不同；本轮未据此断言现有服务故障。API 健康检查通过。

原数据库有 17 条迁移、1 个账号、0 张订单。现有配置为 HTTP，关闭了安全 origin 和启动管理员门槛。新版本切换时应明确访问与会话配置，不能仅根据镜像构建通过宣称员工登录已验收。本轮镜像内管理员预检证明当前隔离副本有 1 个有效特权账号。

独立工作目录为服务器上的 `/home/ubuntu/aerolink-trial-preflight-20260910`，没有覆盖 `/home/ubuntu/aerolink-prod`。隔离 PostgreSQL 容器为 `aerolink-trial-migration-20260910`，仅绑定服务器 `127.0.0.1:55470`。验证结束已停止容器、关闭本机 SSH 转发，备份与验证库保留。

## 实际服务器数据库升级演练

1. 对现有 PostgreSQL 执行无 owner/ACL 的 custom-format 备份，在权限受限目录保存。
2. 将备份恢复到独立 PostgreSQL 16 容器。
3. 使用候选 `a4cc4896ec60dae9a2debdc4818565660552291f` 的 Prisma 5.22.0 执行 migrate deploy，从 17 条升级至 41 条，全部成功。
4. Prisma schema diff 为空；库存、金额、JSON 配置、状态四类只读对账通过。四类业务样本均为 0，不表示已覆盖有量历史交易。
5. 原数据库仍为 17 条迁移，原 API/Worker/网页持续运行，API 健康检查再次通过。

备份 SHA-256：`9b0335b26a9a3dc7d9b7bfe1d08b51e7c83bb1b731d16278c45d6692569b3474`。备份仅留服务器，不下载或提交。本次只验证数据库备份/升级，不替代数据库与附件的一致恢复验收。

## 发布镜像已构建

源码从精确 Git SHA 归档，排除未跟踪文件，在传输后校验 SHA-256：`b6ca37f7f2d27a3aa497917363dd757d5ffb6d32f86ff5a0c167fd576afdcc44`。

使用候选原始 `deploy/Dockerfile.server` 和 `deploy/Dockerfile.web`，未修改 Dockerfile，未关闭 TLS 或包校验，使用服务器已有的 Docker registry mirror。构建复用了已有基础层，因此不能将本轮通过解释成本机此前 APT 证书故障已修复。

| 产物 | 结果 |
|---|---|
| `aerolink-trial-backend:a4cc4896ec60dae9a2debdc4818565660552291f` | 构建通过，镜像 ID `sha256:3fc48d6a21b2133001585db166d4ed1e251de180d781e5ebc6840a0e26a2554b` |
| `aerolink-trial-web:a4cc4896ec60dae9a2debdc4818565660552291f` | 构建通过，镜像 ID `sha256:897d46ffb4ef26a14b03c871e1a331804bdb542cae68baace26047b382e763fb` |
| 后端真实运行时 | Node `v22.23.2`，候选 `pdfService` 调用镜像内 `/usr/bin/chromium` 生成 84,348 字节 PDF，浏览器关闭正常 |
| 镜像内数据库启动预检 | 订单唯一性通过；有效特权账号检查通过 |
| 前端 Nginx | `nginx -t` 通过，未绑定服务端口 |

PDF SHA-256：`b96918aeffb1b6d31b77912e26fc086862ca8c959bc26dfd75f9e2221e8c5da4`。PDF 检查在无网络的一次性容器内运行，只保存合成内容，不连接现有业务服务；证明生成能力，不构成页面内容或中文排版的视觉验收。

服务器证据：独立目录内 `backend-build.log`、`web-build.log`、`runtime-evidence/`。本机迁移和对账日志：`C:/Users/jiaxi/AppData/Local/Temp/aerolink-server-clone-*-20260910.log`；构建日志副本为同目录的 `aerolink-server-backend-build-20260910.log` 与 `aerolink-server-web-build-20260910.log`。

## 当前剩余

Docker 镜像构建这一阻断已在指定服务器上取得通过证据。GitHub 读取恢复，但远端 main 仍为 `acab49917c516b016576eed147b5ce83562cbc62`，候选 SHA 尚不存在于远端。

已向用户请求将精确候选推送至 codex 分支、创建草稿 PR 和触发验证的授权，尚未收到答复。按现有发布流程，仍需同一候选的 CI 和 Build and Verify 产出的发布凭证；不能把本地或服务器日志伪装为 GitHub artifact。

取得发布验证凭证后，继续试用环境切换与真实员工岗位验收。D15、D16、D18 保持后置，D17 不扩大范围。

## 推送前有限内容复核（2026-09-11）

Luna/max 子 agent 对基线 `acab499` 至候选 `a4cc489` 的新增/修改行做有限扫描，排除 vendor/minified 及未跟踪环境文件，未确认真实凭据泄露。这不是全历史 Gitleaks 验收，不能替代远程 secret scan。

root 复核了标记项：采购浏览器密码明确用于显式开关和本机特定隔离库；Outbox 邮箱样例沿用既有基线中的 fixture，邮件发送函数被 mock，未发现本批引入真实联系人数据的证据。子 agent 标记的 release 合同脚本内联认证 URL 未能在当前文件中复现，不将该项记作发现。

PR 正文已准备在本机临时文件 `aerolink-candidate-pr-body-20260911.md`，尚未创建远端 PR。候选保持原 SHA、工作区干净；没有修改已验证源码或重新运行无必要的测试。发布所需的推送/工作流授权仍待用户回复，当前无继续运行的构建或验证任务。
