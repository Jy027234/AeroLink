# P2-09 移动端、无障碍、文档与总验收首批

日期：2026-07-19（Asia/Shanghai）
状态：进行中（Node 22 全量门禁、三档 viewport/axe、Settings、键盘焦点、错误提示公告、移动阅读顺序和 44 条全量 E2E（含高缩放预检）已通过；人工高缩放/阅读顺序签核仍待收口，Worker drain 发布合同已通过）

当前已具备 Query/Feature、Worker、对象存储、观测和 Settings URL 的首批交付记录。`e2e/p2-responsive-a11y.spec.ts` 已在 360/768/1440 三档覆盖 dashboard、RFQ、报价、订单、库存、供应商信息和 Settings，页面级横向溢出与 axe critical/serious 门禁 3/3 通过；本轮修正全局加载状态文字对比度后，桌面 axe 门禁恢复通过。`e2e/p2-settings-url.spec.ts` 通过 2/2；`e2e/p2-keyboard-focus.spec.ts` 覆盖移动抽屉焦点圈定/恢复和账户菜单 Escape 恢复 2/2；`e2e/p2-error-announcements.spec.ts` 验证登录与找回密码校验错误以 `role=alert` 公告 2/2；`e2e/p2-reading-order.spec.ts` 在移动端验证 banner→main 结构和前 20 个可见、有名称 Tab 停靠点 1/1；`e2e/p2-high-zoom-preflight.spec.ts` 在 1280×800 下以 200%/400% CSS 缩放覆盖核心页面并通过 2/2。Node 22.16.0 下正确启用 `FEATURE_AGENT_DEMO=true` 后根/后端门禁与隔离全量 Playwright 44/44 通过。本包仍保留 [人工高缩放与阅读顺序清单](P2-09_人工高缩放与阅读顺序清单_2026-07-19.md)，Worker drain 发布合同见 [P2-05 滚动发布与回滚演练](P2-05_滚动发布与回滚演练_2026-07-19.md)。

## 验收证据

```powershell
$env:E2E_PASSWORD='ci-demo-password-2026'
$env:PLAYWRIGHT_EXTERNAL='true'
$env:PLAYWRIGHT_BASE_URL='http://127.0.0.1:5174'
$env:PLAYWRIGHT_API_ORIGIN='http://127.0.0.1:3310'
$env:FEATURE_AGENT_DEMO='true'
npx playwright test e2e/p2-responsive-a11y.spec.ts --project=chromium
# 3 passed (mobile/tablet/desktop)
npx playwright test e2e/p2-settings-url.spec.ts --project=chromium
# 2 passed
npx playwright test e2e/p2-keyboard-focus.spec.ts --project=chromium
# 2 passed
npx playwright test e2e/p2-error-announcements.spec.ts --project=chromium
# 2 passed
npx playwright test e2e/p2-reading-order.spec.ts --project=chromium
# 1 passed
npx playwright test --project=chromium
# 44 passed
```

明确边界：不新增外部供应商门户、真实 ERP/物流/邮件/价格数据接入、域名/TLS 或公网生产切流。

## 回滚与下一步

本批无数据库迁移。若移动端/高缩放修复引入回归，可按页面批次回退 `src/components/Layout`、`src/hooks/use-mobile.ts`、`src/index.css` 及对应页面包装属性；保留表格明确横向滚动和原有业务主链。回退后重跑三档 viewport/axe、Settings、键盘焦点、高缩放预检和全量 Playwright。P2-09 仍不得在人工高缩放、阅读顺序和屏幕阅读器清单签核前标记为全部完成。
