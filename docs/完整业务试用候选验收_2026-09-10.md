# 完整业务试用候选验收（2026-09-10）

后续更新：用户已指定测试服务器，候选前后端镜像均在该服务器构建成功，镜像内 PDF 与数据库启动预检通过；现有库备份恢复后的 17→41 条迁移演练通过。详见 [测试服务器发布预检](测试服务器发布预检_2026-09-10.md)。本文下方本机构建失败保留为过程证据；当前构建已不再是目标服务器的未完成项，远程 CI 凭证与服务切换/员工验收仍待完成。

## 距离员工试用

首期范围仍为自有库存销售、背靠背采购转售、USD 和既定审批规则。采购、交付、结算必要页面及混合交付主链已有本地验证证据。当前剩余两道门槛：

1. 同一候选完成发布镜像与发布验证，部署到试用环境，验证配置、迁移、附件/PDF、API/Worker 和岗位权限。
2. 真实员工按销售、采购、质量、仓储、财务岗位走脱敏样本，修复阻断问题后开放少量员工试用。

若纳入真实历史数据，另须取得该批数据库和附件的隔离恢复证据。合成数据试用不因此扩大开发范围。尚未部署，不能宣称已经可供员工登录试用。

D15 服务端助手、D16 AI 草稿和 D18 兼容退出留在后续优化，不作为试用门槛；D17 当前只交付必要岗位入口。完整长期目标保留，不继续扩大首期范围。

## 冻结候选

- SHA：`a4cc4896ec60dae9a2debdc4818565660552291f`。
- 分支：`codex/complete-trial-candidate-20260910`。
- 独立目录：`C:/Users/jiaxi/AppData/Local/Temp/aerolink-complete-trial-candidate-20260910`。
- 41 条迁移；候选源码和工作区保持干净。本记录仅写入主开发分支。
- 旧 `4ab3b1c` 候选保留为较窄范围的历史证据，当前完整首期发布验证以本候选为准。

## 本地证据

固定 Node 22.23.2、npm 10.9.8，前后端分别按锁文件干净安装，独立生成 Prisma 5.22.0 客户端；Vitest 实际版本均为 4.1.11。

| 检查 | 结果 |
|---|---|
| 前后端生产构建 | 通过 |
| 后端单测 | 152 文件、862 项通过 |
| 前端单测 | 27 文件、100 项通过 |
| 官方 registry 审计 | 前后端生产/开发四份报告均为 0 漏洞 |
| API 严格覆盖 | 359 operations、60 mounts、0 inventory-only；保留 3 个关闭入口无 2xx 警告 |
| 生成类型、vendor、CI 合同、模块边界 | 通过 |
| 空库迁移及 schema diff | 41 条迁移通过，diff 为空 |
| 常规浏览器全套 | 59 通过、1 失败、2 跳过；失败文件串行复验 2/2 通过 |
| 专用采购至交付、结算浏览器流程 | 1/1 通过，独立质量岗位、真实 PDF 下载及窄屏检查通过 |
| 浏览器后数据库对账 | 库存、金额、JSON 配置、交易状态四类通过 |

常规浏览器失败为动态加载 `src/sections/Quotations/index.tsx` 失败。当时同一候选目录运行了两个 Vite 实例；停止并行实例后串行复验通过，但尚不能证明并发缓存就是原因。不能将本次结果写成“全套一次通过”。两个跳过项为采购/结算显式开关用例，另由专用完整流程覆盖。

第一次浏览器环境准备遗漏 `NODE_ENV=test` 及显式 API origin，造成测试账号未生成和部分请求误用默认端口；发现后停止，另建空库正确迁移及 seed，没有改写原失败库。该次不作为产品验收结果。

正确常规数据库为 `aerolink_trial_complete_e2e_fixed_20260910`。最终只读对账覆盖库存 15 件号、金额 56 记录/134 字段、JSON 10 值、状态 83 记录，均无不一致。以上是合成数据，不代表真实历史数据已通过。

## 完整交易样本

专用数据库 `aerolink_settlement_test_20260910`，样本标记 `7defca60e72e`，订单 `eececb46-418e-48c3-b1ee-53632a98161a`。

同一订单直发 2 件、本地出库 1 件，3 件全部签收，订单为 DELIVERED。应收原额 12,000，收款、退款及信用冲减后的有效收款和调整应收均为 11,999；应付 7,500 已结清。未收、未付及待退款均为零。浏览器后发运对账覆盖 25 张现代订单、5 批本地发运、19 批直发，issues 为空。

流程从合成且满足商业约束的报价/订单事实开始，采购以后的关键动作走页面；不宣称从 RFQ 录入开始的每一步都已在该用例覆盖。采购件退货后复售的页面交叉场景仍未覆盖。PDF 使用本机显式 Chromium 验证，不能替代最终发布镜像内的 Chromium 验证。

## 发布阻断及证据位置

从本候选按实际 `deploy/Dockerfile.server` 构建两次，均未产出发布镜像：第一次默认 APT 镜像重定向后证书信任失败；第二次仅使用已有构建参数指定 Debian/Prisma 官方镜像，但在 Node 基础镜像元数据获取阶段遇到 Docker Hub TLS handshake timeout，尚未进入 APT 步骤。没有关闭 TLS、APT 签名或完整性检查；第二次不能证明 APT 问题已解决。前端镜像本轮未构建。

尚无该候选远程 CI、发布清单与目标环境运行证据；未推送、部署或改动生产数据。

日志均位于 `C:/Users/jiaxi/AppData/Local/Temp/`：

- `aerolink-complete-candidate-*-20260910.log`：安装、构建、测试、迁移、浏览器和对账。
- `aerolink-complete-trial-candidate-audit-20260910/audit-summary.json`：候选 SHA、运行时、四层审计及合同检查。
- `aerolink-complete-candidate-chain-results-20260910/`：完整交易浏览器附件。
- `aerolink-complete-trial-docker-backend-build-native-20260910.log`：默认构建失败。
- `aerolink-complete-trial-docker-backend-build-debian-mirror-20260910.log`：官方镜像参数构建失败。

下一步只处理发布通路和员工样本验收中的实际阻断，不启动 D15、D16、D18。
