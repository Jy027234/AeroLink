# AeroLink 优化执行记录

持续目标：执行《报告复核与下一步待办》的 D00–D18，优先完成受控内部试用安全门槛，随后完成业务闭环与服务端助手。用户已授权实施及子 agent 协作；协作模型统一为 Luna / max。当前不代表已部署或全部验收。

## 当前距离试用（2026-09-10）

**2026-09-11 最新状态：**用户已授权推送和运行验证。草稿 PR #2 的最终候选 `681c6a5` 已通过完整远程 CI、Release Gate 和 Build and Verify；测试服务器上的同版本镜像、实际环境 Compose 配置及发布凭证已就绪。远程验证期间仅修复默认 APT 引导源及两处 E2E 硬编码端口。尚未合并 main 或切换现有服务，下一步为候选服务切换及员工岗位验收，见 [D01 远程候选验证](D01_远程候选验证_2026-09-11.md)。以下“等待推送授权”等描述保留为早期阶段记录。

**测试服务器进展：**指定服务器上的前后端候选镜像已构建成功，镜像内 PDF/启动预检通过，现有库的隔离副本已从 17 条迁移升级到 41 条且 schema diff 为空。原服务保持健康、原库仍为 17 条迁移。当前等待候选推送/远程验证授权，之后继续发布切换及员工验收；详见 [测试服务器发布预检](测试服务器发布预检_2026-09-10.md)。

**最新状态：**完整首期候选已冻结为 `a4cc4896ec60dae9a2debdc4818565660552291f`，在独立目录按锁文件干净安装后，前后端构建、后端 862 项、前端 100 项、四层零漏洞审计及四类数据库对账通过。常规浏览器为 59 通过、1 次模块加载失败、2 跳过；失败文件串行复验 2/2 通过，专用采购至混合交付及结算流程 1/1 通过。剩余是发布镜像/试用环境验证和真实员工岗位走单，详见 [完整业务试用候选验收](完整业务试用候选验收_2026-09-10.md)。下方旧候选与阶段进展保留历史含义，不再代表最新候选。

D15、D16、D18 是原计划中的后续优化，不是首轮试用的新门槛；D17 仅补当前业务必需入口。近期不领取助手增强或兼容退出任务。

- **少量员工试用已有人工流程：**冻结候选 `4ab3b1c` 的本地验证已完成；还需完成该候选的镜像/发布验证及测试环境部署，再由员工按岗位走样本流程。当前尚无已部署的试用服务。最近发布验证受 Docker Hub / GitHub TLS 故障影响，不能按代码测试通过推定可上线。
- **首轮同时覆盖背靠背采购至结算：**必要页面及同一订单的混合交付、结算浏览器验证已完成；最终样本为两件直发、一件本地出库，全部签收且应收/应付/待退款结清。该增量已进入独立完整候选 `a4cc489` 并完成本地复验，仍需发布及员工验收。
- **真实历史数据：**只有在纳入历史数据时，才需先取得该批数据及附件的恢复证据；不把它扩成纯合成数据试用的新增开发项。

以下早期阶段记录保留当时状态；当前剩余范围以上述清单及最新 D14 验收为准。

本轮发布完整性修复 `f0e3f65` 补齐 API、Worker、网页实际运行容器的镜像 ID 核对，成功、旧镜像拒绝及回滚合同测试通过。该修复已包含在完整候选 `a4cc489`，未修改旧候选 `4ab3b1c`，未部署。它落实原 D01 的版本一致性要求，不新增待办编号。

## D14 结算增量（2026-09-10，本地页面验证完成）

- 已接入应收/应付来源快照、外部凭证、收付款、信用冲减、退款、全额冲销及账期调整接口。新 016 迁移仅应用于独立结算合成库，共 41 迁移；冻结试用候选仍为 `4ab3b1c`。
- 真实认证 HTTP 和 PostgreSQL 已通过收款→冲减→退款→冲销、权限收紧后幂等重放拒绝、并发单赢家、重复凭证回滚及历史金额保护验证。财务登记/冲销页面、销售应收视图、真实 PDF 上传下载和 390px 页面检查均通过；随后完成同一订单的采购至交付、结算验收，并修复采购库存预留漏传收货来源。最新后端 862 项、前端 100 项通过，见 [结算凭证与冲销验收](D14_结算凭证与冲销验收.md)和[完整交易岗位验收](D14_完整交易岗位验收_2026-09-10.md)。目标环境与真实员工验收仍待完成。
- 发布通路 2026-09-10 只读复查仍受 Docker Hub / GitHub TLS 握手故障阻断，没有推送或部署；候选目录仅追加验收文档，源码未改变。D15、D16、D18 仍未领取。

## D14 直发增量（2026-09-09，历史阶段记录）

- 试用优先级已收口：M1 候选发布落地优先；D15、D16、D18 不作为首轮试用门槛。完整 D00–D18 目标保留，D17 仅随业务链补必要岗位入口。
- 本轮已接入订单内采购/收货/直发页面，并完成真实浏览器建采购→独立审批→供应商确认→两批直发→独立质检→签收，以及另一个序号行收货待检→独立验收入库。质量账号实际下载凭证、390px 弹窗宽度检查通过。前端全套 89 项及构建、契约/边界、浏览器后有量发运对账通过，详见 [D14 直发验收](D14_供应商直发验收.md)。
- 独立试用候选仍为 `4ab3b1c61c9dd3d6997abd249884953acc15932f`，worktree 干净。下述直发增量未混入候选；D15、D16、D18 尚未领取。
- 已接入直发计划、独立质量审核、发运、取消、分次签收 API，并适配本地与直发混合交付、附件范围、采购取消及数量对账。最终后端全量 149 文件 / 842 项通过，两端 TypeScript、契约和边界检查通过。
- 独立测试库当前 40 迁移。有效商业夹具、独立性反例、真实认证与并发/幂等主链和浏览器后有量发运对账均通过；整库交易行对账中早期负面测试数据仍保留阻断，不宣称完整 D14 已交付。
- 完整记录与失败边界见 [D14 供应商直发验收](D14_供应商直发验收.md)。应收应付凭证与冲销、目标环境及真实岗位验收仍待完成。

## 首轮试用收口（2026-09-09）

用户询问距离试用还有多少，并指出 D15–D18 容易被理解为追加范围。D15–D18 已存在于最初复核待办的“批次 3”，总体目标保留，但不将全部优化完成设为首轮试用前提。近期优先交付可试用候选版本，暂不领取 D15、D16、D18；D17 仅纳入试用动作必需的页面入口，不前置完整工作台改造。

| 试用范围 | 当前依据 | 剩余交付边界 |
|---|---|---|
| 少量员工受控试用已有人工交易流程（M1） | 权限、审批与 D10–D13 已有本地 API、数据库和浏览器验收；最近已提交基线为 `333140d`，收货阶段后端 141 文件 / 768 项通过 | ① 收口并冻结候选提交、核对开放入口与角色；② 同一候选的 CI/镜像及目标测试环境验证；③ 员工按岗位走完样本流程并关闭阻断问题。当前尚未部署，不称为已经可供用户试用。 |
| 完整覆盖已确认的自有库存 + 背靠背范围（M2/M3） | 采购承诺和收货/独立验收后端已交付；直发增量仍在工作区，未计入已验收成果 | ① 受控直发发运与签收闭环；② 应收应付外部凭证、收付款及冲销摘要；③ 采购/质量/财务必需页面；④ 完整交易的岗位浏览器验收，并满足上述发布门槛。真实历史数据投入使用前补齐 D09 恢复证据。 |

这两种试用范围不能混称：先开放已有流程不表示背靠背已完整交付，也不取消用户确认的背靠背目标。D15–D16 的助手增强和 D18 兼容退出属于后续优化；它们不是当前试用阻断项。不按待办编号或自动化测试数量推算百分比，也不在目标环境和岗位样本尚未验证时承诺具体上线日期。

收口项仍归入现有 D05，不增加待办编号：已确认挂载层仅认证，证书 `renew/revoke` 原来缺少创建接口的 `certificate.issue` 能力门槛。现已补齐两条入口的能力校验；撤销要求明确原因并在 Serializable 事务内按当前状态和更新时间比较后更新，保留旧追溯历史并记录当前操作者。直接修改日期使旧证书重新有效的续期入口已关闭，授权调用返回 `409 QUALITY_EVIDENCE_REQUIRED`，不查询或修改证书；页面移除续期按钮，并按能力显示签发和撤销入口。取得新有效证据后仍须重新审核，不把日期编辑当作新证据。

本项定向验证：Luna/max 子 agent 的 Express + 真实能力策略测试 12/12 通过（数据库使用 mock；覆盖角色拒绝、续期无读写、伪造字段拒绝、撤销审计与 CAS 冲突）。root 已完成前端生产构建、后端 TypeScript 检查、OpenAPI 严格覆盖及生成类型一致性检查；347 operations / 58 mounts / 0 inventory-only，3 个明确关闭入口的无 2xx 警告。以上尚不替代独立候选版本的完整回归、数据库运行和目标环境发布验证。

## 已确认的首期规则

2026-09-08 用户明确确认：

- 自有库存销售 + 背靠背采购转售。
- USD 单币种；不默默把其他币种金额当作美元。
- 金额不超过 5,000 由经理审批，超过 5,000 且不超过 50,000 需财务，超过 50,000 需 GM；高一级授权可处理低额度，AOG 不绕过额度。
- 默认禁止自批。

具体公司证据清单、税运费规则、ERP 库存主账关系、审批额度动态配置和兼岗仍需要在后续业务配置中明确；本批不臆造适航标准，也不将历史资料默认视为审核通过。

## 实施状态

| 待办 | 当前进展 | 仍需完成的证据/工作 |
|---|---|---|
| D00 | 上述规则已确认；新交易仅 Sale，未覆盖销售类型不能审批/履约 | 公司质量要求、财务口径和库存系统关系 |
| D01 | CI 功能与审计分离，发布构建也固定 Node 22.23.2；独立候选更新安全依赖后干净安装成功，后端 780 / 前端 80 / 浏览器 60 项通过，四层在线审计为 0、四类数据库对账通过 | Docker 镜像拉取仍遇到 registry TLS 超时；最终候选 SHA 的远程 CI、目标环境和员工验收，见试用候选记录 |
| D02 | 浏览器任意 runtime PUT 禁用；历史任务只供管理员查看，始终标为不可信；只读助手 E2E 通过 | 可信任务归属在 D15 建立独立服务端事实 |
| D03 | 报价/订单/审批及嵌套响应统一投影；补齐库存和报表成本权限、文档当前归属检查、幂等重放脱敏 | 空值显示已修复；新增交易链的最终角色回归 |
| D04 | 实际额度/角色、自批限制及审批内容快照；旧审批需重新审核；交易 E2E 通过 | 正式环境策略配置 |
| D05 | 独立交付审核记录、证据版本绑定、出库消费审核、封闭手改发货、保护订单质量字段；API/UI 与真实并发出库通过 | 直发尚未开放；质量规则后续按公司制度扩展 |
| D06 | 当前身份/单据范围投递；独立 API/Worker 通道与过期断连、刷新重连验证通过 | 浏览器实际自动取回状态与生产部署验收；不支持多 API 副本 |
| D07 | 合法续期按 user/session；31 个同出口合法会话不消耗 IP 桶；异常请求仍限流 | 真实员工并发规模和代理部署验证 |
| D08 | 供应商精确匹配/歧义处理，人工交接与未发送不再计为执行成功；助手 E2E 通过 | 后续窄执行接口继续使用真实产物判定完成 |
| D09 | 当前 19 条迁移、真实 Prisma 模型合成交易与附件的隔离备份恢复、库存/金额/文件哈希对账通过 | 真实历史备份与生产对象存储的隔离恢复证据 |
| D10 | 多行 RFQ、选择行询价、逐行报价/成本快照、整单审批、分批成交生成多订单及逐单合同已实现；真实 PostgreSQL、HTTP 与浏览器草稿提交验证通过 | 与 D12/D13 逐行实物分配、质量审核、分批出库继续衔接；现代订单旧整单履约入口关闭 |
| D11 | 商业版次、独立修订草稿与强制重审、旧版写入阻断、公开文件冻结及原 PDF 字节重用已实现；真实 PostgreSQL、HTTP 和浏览器修订验收通过 | 真实历史日期/原始文件对账 |
| D12 | Allocation + Assignment、数量 API、质量出库、到期释放、历史身份保护和页面入口完成；后端 117 文件 / 535 项、前端 22 文件 / 79 项、HTTP/浏览器联合 4 项通过，真实 PostgreSQL 并发和对账通过 | 真实历史分配不自动回填；多批运单及受控退货放行由 D13 完成，见 D12 验收记录 |
| D13 | 多批运单、逐行签收、退货隔离、独立质量放行及序号件复售已实现；后端 123 文件 / 606 项、HTTP/原有 UI 5 项及新双岗位 UI 1 项通过，真实 PostgreSQL 数量对账通过 | 真实历史运单不自动回填；供应商直发和商业冲销由 D14 衔接，见 D13 验收记录 |
| D14 | 采购、收货、直发和结算受控命令及页面已接入；同一订单混合交付及结算的真实浏览器流程、PDF 下载、41 迁移独立库 HTTP 和并发验证通过 | 最终候选发布及真实员工验收；采购件退货复售的页面交叉场景未在本轮覆盖，不宣称全部业务场景已验收 |
| D17 | 订单内采购、收货、直发及结算必要入口随 D14 交付 | 完整交易上下文、每日待办与真实员工任务验证；不先重做全部导航 |
| D15、D16、D18 | 未领取，不阻挡 M1 受控试用 | 服务端助手、AI 草稿链路、兼容层退出 |

2026-09-09 试用收口已形成独立候选，详见 [受控试用候选验收](受控试用候选验收_2026-09-09.md)。已验证程序源码为 `b8a6657`，35 迁移；原工作区未验收的直发 011 增量未进入候选。当前本地验证通过，不代表已推送、部署或完成完整背靠背业务。

## 已取得的验证证据

- 第一次合并前的相关后端检查：6 文件/36 测试通过（Node 24，仅基线观察）。
- 质量审核新规则与既有服务/幂等回归：4 文件/30 测试通过。
- 安全批次基线（D10/D11 后续扩展前）的后端全量：Node 22.23.2，90 文件 / 318 测试通过，后端类型检查通过；包含 Sale 范围、上传持久化、恢复工作目录安全等新增回归。D10/D11 当前工作树另行回归，不能以此旧结果替代最终验收。
- 已在独立 Docker PostgreSQL 16 上从空库运行完整迁移，包含 `20260908000100_fulfillment_reviews` 与 `20260908000200_quotation_approval_policy`。没有执行生产数据库迁移。
- `server/src/scripts/checkFulfillmentSafety.ts` 已在 Node 22.12.0 与真实 PostgreSQL 下通过：无审核阻断、手改 SHIPPED 阻断、普通质量字段修改阻断、两个并发出库仅一个成功、附件版本变化失效旧审核；库存数量与出库流水核对正确。该脚本测试数据库证据元数据，不代表已验证真实文件备份恢复。
- OpenAPI 严格路由覆盖：318 操作、54 挂载、0 inventory-only；生成类型一致。停用的旧同步接口仅返回错误，因此 lint 有“缺少 2xx”提示；契约如实保留 410，不虚构成功返回。
- 前端全量合并检查：Node 22.23.2，18 文件 / 66 测试通过；包含实时订阅 hook，旧 runtime 写接口的契约断言已按停用语义修正。
- 实时订阅、报价重审界面、Sale 范围限制及本地 PptxGenJS 依赖全部集成后的前端生产构建通过。
- 实时连接 hook 和 OpenAPI 定向回归 2 文件 / 20 项通过；独立进程及共享出口限流证据见 [D06/D07 验收记录](D06_D07_实时与限流验收.md)。
- 6 个交易 E2E 文件 / 16 项通过，见 [D03–D05 交易安全验收](D03_D05_交易安全验收.md)。上传新源码重启后，质量审核面板与只读助手页面 2 项 E2E 再次通过；专用上传暂存目录为空，已核对并清理旧代码遗留的 7 个测试副本，正式对象存储中的相同文件保留。
- 本次隔离测试库的库存、金额、JSON 配置及状态 shadow 对账通过；其中 JSON 配置样本为 0，只表示本次没有该类记录，不能据此宣称覆盖历史配置迁移。
- D09 当前应用 schema 恢复演练通过：19 条迁移、193383 字节 PostgreSQL dump、2 个关联文件，业务模型、库存、金额及文件哈希一致，备份后篡改被隔离；见 [恢复演练记录](D09_PostgreSQL文件恢复演练.md)。这仍是合成业务证据，不代替真实历史备份验收。
- D10 新增只读预检 5 项测试通过。在经历 E2E 的隔离库上检查 13 个 RFQ、22 个报价、11 个订单及 2 个供应商报价，返回 10 个报价与 RFQ 件号不一致阻断项、1 个孤立供应商报价待核项；没有回填或修改任何记录。该结果证明拦截有效，不能表述为历史迁移已通过。
- RFQ/询价与显式替代件号预检的后续定向回归：5 文件 / 16 项通过。新的 `aerolink-inquiry-test-20260908-1` 独立 PostgreSQL 容器通过 21 条迁移；API 验证了源行/备注/DRAFT 落库、相同幂等键不重复、跨 RFQ 行拒绝、他人单据访问拒绝和 send 返回 409 后状态不变。浏览器草稿弹窗明确“尚未发送”；成本隐藏造成的仪表盘空值渲染问题已修复，默认 seed 浏览器回归随后通过。
- 当前 D10/D11 扩展第一次后端全量：103 文件 / 363 项中 349 通过、14 失败。1 项 RFQ mock 未模拟 Prisma 嵌套行返回，修正后已通过；其余为成本来源接入与旧测试样本不完整；后续已修复，并保留缺来源等反例。本次失败记录不作为最终验收结果。
- 后端独立干净安装与构建：Node 22.23.2 / npm 10.9.8，334 包安装、Prisma Client 5.22.0 生成和 TypeScript 构建通过；验证未设置数据库连接、未触碰运行库。日志在 D01 临时证据目录的 `backend-ci-v22/backend-build-evidence.json`。
- 本地分支 `codex/internal-safety-20260908`；尚未形成远程候选提交或部署。
- 后端依赖更新后 `npm audit` 为 0；Express 使用修复后的 `qs 6.16.0`，Puppeteer 升至 25.10.0 并适配 `Browser.connected`、`setContent` 等 API。真实 Chromium 生成报价 PDF 并正常关闭浏览器通过（80KB PDF，正确文件头）。
- 前端兼容依赖更新后曾剩余 `pptxgenjs → image-size` 两个高危项。已核对官方 PptxGenJS 4.0.1 的发行代码不引用该图片库，并校验官方 tarball SHA-512 后保存未修改的运行代码、类型和许可证，仅修正包依赖声明，实际移除易受攻击的依赖。`vendor/pptxgenjs/UPSTREAM.json` 固定逐文件 SHA-256，CI 校验文件、依赖缺失及 ESM/CJS 生成能力。原版与精简依赖原型的完整 15 页生成结果共 106 个 ZIP 条目在时间/临时路径归一化后一致。仓库集成后在线 `npm audit` 为 0；没有降级、删除文稿功能或关闭审计。
- D01 临时可复核证据：`C:\Users\jiaxi\AppData\Local\Temp\aerolink-d01-vendor-ci-20260908-124512\audit-run.json` 记录 Node `v22.23.2` / npm `10.9.8` 下根目录 `npm ci`、vendor 校验及前端/后端 production/development 四层官方 registry 审计，四层均为 0 个漏洞；明细在同目录 `ci-evidence/frontend` 和 `ci-evidence/backend`，根安装、vendor 校验和 workflow contract 日志分别为 `root-npm-ci.log`、`vendor-check.log`、`workflow-contract.log`。`Dockerfile` 与 `deploy/Dockerfile.web` 均在 `npm ci` 前复制 `vendor/pptxgenjs`，`.dockerignore` 保留其 `dist`/types/manifest 并排除内部临时 `node_modules`。本地前端 Docker 构建已尝试两次，均在拉取 `node:22.23.2-alpine` 时被 Docker registry TLS handshake timeout 阻断，日志见 `docker-frontend-build.log` 与 `docker-frontend-build-retry.log`；未用不符合 Node 22 基线的本地 Node 20 镜像替代，未运行或推送镜像。

## 本轮前端合并验收（2026-09-08）

- 使用隔离 Node `v22.23.2`（路径 `C:\Users\jiaxi\AppData\Local\Temp\aerolink-node22-22.23.2\node-v22.23.2-win-x64`）运行根目录 Vitest：`20` 个文件、`70` 个测试全部通过。
- 前端生产构建 `npm run build` 通过：TypeScript project build 与 Vite 均成功，Vite 转换 `2629` 个模块，耗时 `21.09s`。
- `npm run api:check:strict` 通过，路由覆盖为 `318` operations、`54` mounts、`0` inventory-only，生成客户端一致。Redocly 保留两个已知警告：runtime PUT 仅返回 `410`、`inquiries/:id/send` 仅返回 `409`，均是停用/人工流程阻断语义，不虚构 `2xx`。
- `features:check` 通过（`7` 个 public feature entry points）；`modules:check` 通过（`4` 个 server module public entry points）。
- D03 空值 UI 已覆盖库存详情、列表、选中总值和 Sourcing：成本不可见时显示 `—`，不把缺失成本当作 `0` 或参与聚合；编辑隐藏成本库存时提交省略 `unitCost`，避免覆盖真实成本。全量 Vitest 已包含该回归。
- `e2e/inquiry-source-lines.spec.ts` 已纳入普通 seeded E2E；无临时 fixture 时使用 `NODE_ENV=test` seed 的 `sales-test@aerolink.com`、`test-sales-peer@aerolink.com`、`c001`、`s001/s002`，密码仍从 `E2E_PASSWORD` 获取；显式 `AEROLINK_INQUIRY_FIXTURE` 只覆盖专用环境。独立 seed PostgreSQL/API/Vite 验收为 `6/6` 通过，覆盖源行、幂等、跨 RFQ 行拒绝、非 owner 权限、send `409` 状态不变及浏览器草稿“尚未发送”。

### 依赖处置参考

`extract-zip` 尚无修复版本，已通过升级 Puppeteer 移出该依赖链，参见 [安全公告](https://github.com/advisories/GHSA-jmr9-qjv8-65gv) 及 [Puppeteer 25 迁移说明](https://github.com/puppeteer/puppeteer/releases/tag/puppeteer-v25.0.0)。`qs` 修复版本为 6.16.0，参见 [上游公告](https://github.com/advisories/GHSA-4mjr-xmp4-gh2g)。已移除的图片库风险参见 [ICNS 公告](https://github.com/advisories/GHSA-w3rx-r6r6-pgpr) 与 [JXL/HEIF 公告](https://github.com/advisories/GHSA-5p2g-fcmc-qvqq)。内部保存的 PptxGenJS 发行包说明见 [vendor README](../vendor/pptxgenjs/README.md)；后续升级必须重新验证上游来源、依赖使用和生成结果。

## 迁移与行为变化

- 本批最后一轮后端全量：Node 22.23.2，15:29:15 启动，105 文件 / 390 项全部通过，75.69 秒；`server npm run build` 通过。包含最后的源行一致性、历史回填状态映射与快照兼容修复。测试日志保留在 `C:/Users/jiaxi/AppData/Local/Temp/aerolink-final-backend-20260908.log`。
- D11/询价组合浏览器验收：7 文件 / 22 项全部通过，覆盖完整交易、库存预留 UI、四位小数、质量审核 UI、合同与 PDF、状态 shadow 和询价来源；见 [D11 验收明细](D11_商业成本来源验收.md)。独立 API/Vite 与 D11 数据库已清理，没有改动生产环境。
- 主 agent 独立只读检查 D10 专用生命周期库：RFQ、报价、订单各一条对应行，行对账 `PASS` 且 0 阻断/待核；3 件订单先出库 1 件、再出库 2 件，订单 `SHIPPED`、累计 3/3、预留归零；预留、释放、再次预留与两条出库流水均存在。该证据覆盖当前单行交易，不代表多行录入或多批次 Allocation 已开放。

- D10/D11 合并后 Node 22.23.2 后端全量于 15:09:41 启动，105 文件 / 387 项全部通过，耗时 74.18 秒；后端类型检查通过。新双写引入的旧 mock 缺失已补齐，订单金额测试继续执行真实行服务并核对头/行四位小数。之前 14 项及后续 7 项失败均保留为过程记录，不能与本轮通过数相加。
- 行对账补齐 RFQ 证书/交期要求、目标价空值差异、预留/成交/出库量和序号/批次/库存 ID，6 项定向测试通过；供应商当前可供数量变化不等于历史报价来源失效。
- 历史有效期只读检查覆盖 22 张隔离库报价，发现 4 张已到期 seed 报价双日期冲突；没有修日期或延长报价。3 项日期诊断测试覆盖时区等价和截止瞬间，见 [D11 成本来源验收](D11_商业成本来源验收.md)。
- 供应商源行 API 扩展后再次通过 OpenAPI strict（318/54/0，仍仅两条停用接口无 2xx 警告）与前端类型检查。

- 新 `FulfillmentReview` 表初始为空。历史在途订单不会自动获得质量审核；需独立质量人员核对当前实物、计划数量及交付证据。
- 一个审核只对应一次计划出库数量；出库消耗该记录，后续分批发运需要新的审核。完整 Shipment/Allocation 模型在 D12/D13 继续实施。
- 普通订单编辑不再改变检验结论或降低质量要求；目前供应商直发在明确的受控流程完成前保持关闭。
- 旧 APPROVED 报价缺少新策略快照时，不能直接发送/接受；允许在原状态追加合法的新审批。审批内容快照与并发 version 分开，库存预留引起的 version 变化不应使商业审批无故失效。
- 为端到端验证，seed 仅在 `NODE_ENV=test` 时增加独立销售、质量测试用户，使用调用方提供的测试密码；没有创建生产账号。
- 测试环境：独立容器 `aerolink-review-test-20260908`、数据库 `aerolink_review`。运行脚本要求显式测试开关及本机专用数据库名，避免使用默认应用连接。
- 上传先使用独立临时目录，成功落入配置的对象存储后清理临时副本；多文件上传后续失败时撤销此前对象元数据和文件，避免留下错误的“可用”记录。

## 后续执行顺序

D10 多行商业输入、审批与部分成交、D11 商业版次和历史文件、D12 数量分配与逐行质量出库、D13 多批运单/签收/受控退货均已形成实现及验收证据，见 [多行商业闭环验收](D10_多行商业闭环验收.md)、[商业修订验收](D11_商业修订与历史文件验收.md)、[数量分配验收](D12_数量分配验收.md) 与 [发运退货验收](D13_多批发运与退货验收.md)。接下来推进 D14 背靠背采购及结算。现代订单使用行分配入口履约，旧整单入口仍关闭；M2/M3 尚未达标。D01 镜像拉取与真实历史备份恢复证据仍待补齐；全部目标尚未完成。

### D14 收货增量（2026-09-09）

已交付待检到货、独立验收/拒收及补送、库存来源与退货复售池、精确证书和附件链。新增 6 条前向迁移（005–010，原基线 29 条增至 35 条），不回填历史质检事实。最终后端 141 文件 / 768 项、构建、OpenAPI 347/58/0、契约 21 项、数据库差异检查及真实链路通过。详见 [D14 采购收货与质量来源验收](D14_采购收货与质量来源验收.md)。

下一增量按顺序完成：供应商直发的独立质量与签收证据；应收/应付外部凭证及反向事件；采购/质量/财务页面和浏览器验收。D15–D18 仍保留在目标内。当前只有本地实现和合成数据验收，未推送或部署。

## 多行商业批次新增证据

- 服务端合并回归已通过 108 文件 / 419 项，Node 22.23.2；后续发现的 RFQ 变更保护及模板适用性反例已另补测试，最终数量以下一轮收尾记录为准。
- 真实数据库覆盖 3 行需求选 2 行报价、财务审批、两次成交、逐单合同、幂等重放、陈旧版本/超量回滚，以及同一版本并发成交只有一方提交。同一库存被多条报价行引用时按合计数量检查，创建和审批的不足反例均通过。
- 独立只读对账覆盖现代 1 个 RFQ / 3 条需求行、3 个报价 / 5 条报价行、3 个订单 / 4 条订单行，0 阻断、0 待核；历史回填预检计划新增 0 行并明确跳过现代记录。
- 实际浏览器操作从三行需求选前两行、逐行定价、查看 USD 800.00 合计、保存草稿并点击提交审批通过。HTTP 全链路另覆盖销售成本脱敏、两份订单合同和实际 PDF。
- 复查补齐了现代 RFQ 完整行更新不能换客户/复活被引用的关闭行、次行件号搜索、CSV 行明细，以及防止旧商品标量合同模板误用于现代分批订单。全部改动仅在本地，未推送或部署。

## 多行商业批次收尾验证

- 最终后端全量：Node 22.23.2，109 文件 / 431 项全部通过，82.56 秒；后端构建通过。日志：`C:/Users/jiaxi/AppData/Local/Temp/aerolink-multiline-final-backend4-20260908.log`、`aerolink-multiline-final-build4-20260908.log`。之前 423 项回归中的单项超时已单独通过，后续完整回归也已通过；不将过程结果重复相加。
- 前端全量：21 文件 / 74 项通过；TypeScript、Vite 生产构建通过。OpenAPI union 测试分别校验旧标量和现代行请求，包括 USD、成本来源和禁止混用头字段。
- OpenAPI strict 为 318 operations / 54 mounts / 0 inventory-only，生成客户端一致；模块边界 4 项通过。仅保留已知两条停用接口无 2xx 的真实契约提示。
- 最终只读复核发现并修复：现代报价撤回写入非法行状态、完成需求覆盖取消行、毛利比例与供应商来源外键不一致、列表映射遗漏完整行。修复新增回归；真实 PostgreSQL 验证撤回满足约束、完成需求保留取消行，详见 `C:/Users/jiaxi/AppData/Local/Temp/aerolink-multiline-postgres-final2-20260908.log`。
- 新旧交易模式明确互斥，已有旧报价的需求不能通过普通编辑转换模式；三条入口反例均已验证，避免混用不同成交数量规则。
- 9 文件 / 24 项 E2E 首次组合全部通过。最终代码复测 23 项通过，1 项询价浏览器测试因重复样本超出第一页而定位失败；改为先搜索目标需求后，该文件 6 项全部通过。最终多行 HTTP 断言确认次行搜索结果携带完整报价/订单行且销售成本不可见。日志：`aerolink-multiline-final-e2e2-20260908.log`、`aerolink-multiline-inquiry-final-20260908.log`（均在 Windows Temp）。没有将重跑 6 项与组合 24 项相加。
- 末次 Sourcing 次行搜索补齐后再执行前端生产构建，日志 `C:/Users/jiaxi/AppData/Local/Temp/aerolink-multiline-frontend-final-20260908.log`。
- 验收结束后确认测试 API/Vite 已停止、两个本批测试库均无连接，再删除 `aerolink_multiline_test_20260908_root` 和 `aerolink_multiline_test_20260908_reconcile`；原 `aerolink_review` 库保留。没有改动生产库、执行历史回填、推送或部署。

## 商业修订批次验证

- Node 22.23.2 后端全量：111 文件 / 460 项通过，84.88 秒；后端构建通过。随后补齐手工取消邮件与公开文件权限边界，分别追加 21 项 outbox 和 14 项文档访问定向验证，均通过；不与全量结果重复相加。
- 前端全量：22 文件 / 79 项通过；最终 TypeScript 与 Vite 生产构建通过。OpenAPI strict：320 operations / 54 mounts / 0 inventory-only，生成类型一致，保留两项已知停用接口提示；模块/功能边界检查通过。
- 两条新迁移在隔离 PostgreSQL 16 执行成功，当前 24 条迁移。旧 Node 进程占用 Prisma 引擎，独立目录完整生成客户端并比对 DMMF 和引擎 SHA-256 一致；没有停止归属不明的旧服务。
- 真实 PostgreSQL 验证了部分成交修订、剩余数量、原订单合同不变、修订/发送竞争、预留释放及失败回滚、签名清空、冻结 HTML 保护、旧无快照文件审批回滚，以及客户改名后的旧 PDF 字节一致。详见 [D11 商业修订验收](D11_商业修订与历史文件验收.md)。
- 新旧交易组合 E2E：10 文件 / 25 项中 22 项通过；3 项旧测试因新增修订按钮后位置定位失效而点错，已为发送按钮补可访问名称并使用名称定位，这 3 项定向复验全部通过。新的修订 HTTP 流程还验证销售可访问公开文件列表/详情/PDF，并在修订后取得完全相同的原 PDF。
- 过程日志在 Windows Temp：`aerolink-revision-backend-verified-20260908.log`、`aerolink-revision-backend-build-verified-20260908.log`、`aerolink-revision-frontend-build-verified-20260908.log`、`aerolink-revision-api-final-20260908.log`、`aerolink-revision-postgres-verified-20260908.log`、`aerolink-revision-e2e-verified-20260908.log`、`aerolink-revision-e2e-send-verified-20260908.log`。
- 浏览器真实修订发现并修复两个问题：空收货地址应省略而非提交 `null`；脱敏递归必须保留 Decimal 的精确 JSON 序列化，不能暴露其内部对象。新增反例后，D10 多行 HTTP/UI 与 D11 修订 HTTP/UI 共 4 项全部通过（`aerolink-revision-ui-final3-20260908.log`）；修订表单验证原单价回显、成本重新核实、`3/3` 件改价为 `110/220` 后生成 USD 990 的新版草稿。
- 最后后端全量：112 文件 / 469 项通过，84.80 秒；包含并行开始的 D12 数量守恒纯函数 8 项，其余 461 项为现有应用与 D11 回归。日志 `aerolink-revision-backend-final3-20260908.log`。最后前端构建日志为 `aerolink-revision-frontend-build-final2-20260908.log`。
- 收尾确认本批 3068/5218、3069/5219 的 API/Vite 均停止，上传暂存目录为空、两个修订测试库无连接后，删除 `aerolink_revision_test_20260908_root` 和 `aerolink_revision_test_20260908_e2e`。原 `aerolink_review` 库保留；未迁移生产、改写真实历史数据、推送或部署。
