# AI 寻源工作台开发待办

日期：2026-09-22
代码基线：`940bc4285892354cccd23d2c30cb55a78f472786`
状态：开发中；已完成第五个本地可运行切片，尚未部署或完成真实邮箱/模型联调。2026-09-23 实机只读核查更正：凯奔已运行 PostgreSQL + 独立 Worker，先前 SQLite 部署记录过时。

## 1. 目标与产品定位

用户录入一个或多个件号的需求，选择供应商并发送询价，接收多家供应商邮件中的价格和交期，逐件号比较后确认供货来源。AI 参与需求整理、供应商候选建议、邮件起草、回邮提取和报价分析，业务员能查看过程并随时接手。

将现有“寻源”升级为“AI 寻源工作台”。AI 与人工共用同一份 RFQ、需求行、询价单、供应商报价和执行记录；通过任一入口操作，另一入口都能看到最新状态。此处“发布需求”指向选定供应商发送询价邮件，首期不包含公开供需市场。

本清单细化[既有待办](AeroLink_报告复核与下一步待办_2026-09-08.md)中的 D15（受控助手任务）、D16（邮件与供货比对）及 D17 的寻源工作台部分，复用已建立的 D10 多行模型和 D11 报价来源约束。以下 `SQ-*` 为该专项内部编号，不替换 D00–D18，不改变其他模块的交付结论。

## 2. 当前基线：可复用与待补齐

| 环节 | 当前已有 | 本次需要补齐 |
|---|---|---|
| 需求录入 | RFQ、RfqLine 及多行录入；AI 邮件需求提取建议 | 多行提取结果一次确认成同一需求；工作台内展示和继续处理 |
| 库存与供应商 | 库存查询、供应商档案、候选筛选及跟进记录 | 逐需求行展示实际匹配依据，标明资料不足，供人工调整 |
| 供应商询价 | 多供应商、多需求行询价草稿 | 真实发送通道、发送版本和逐供应商执行状态；当前 `/:id/send` 明确返回待人工处理 |
| 邮件基础设施 | 邮箱配置、IMAP 同步游标与去重、SMTP、OutboundEmail、Outbox 和独立 Worker | 询价邮件关联、回复线程匹配、附件原件保存及报价提取待确认流程 |
| 供应商报价 | 价格、USD 币种、数量、交期、有效期以及 RFQ/询价行关联 | 来源邮件与提取记录、人工确认、必要的供货条件字段和修订留痕 |
| 比价与中选 | 报价列表、规则比较、标记中选 | 比价目前按 RFQ/询价聚合；中选也未按需求行隔离，需要一起改为逐行 |
| AI 与任务 | 内置智能体、提示词发布版本、模型管理、调用日志 | 本专项的服务端可信任务、回邮提取能力及业务命令衔接；旧客户端自动执行仍关闭 |

核对入口：

- [RFQ 与询价/报价模型](../server/prisma/schema.prisma)、[询价路由](../server/src/routes/inquiries.ts)、[供应商报价路由](../server/src/routes/supplierQuotes.ts)。
- [邮件收发](../server/src/lib/emailService.ts)、[收件同步](../server/src/lib/inboundEmailSyncService.ts)、[Outbox](../server/src/lib/outboxService.ts)。当前收件逻辑保留附件文件名，不能当作已保存附件原件。
- [寻源页面](../src/sections/Sourcing/index.tsx)、[供应商报价页面](../src/sections/SupplierQuotes/index.tsx)、[旧助手编排](../src/lib/agentOrchestrator.ts)。
- [AI 模块业务入口与边界](AI模块使用与业务接入_2026-09-11.md)。已有 AI 调用能力不等于已经完成自动询价和回邮报价入库。

环境记录：2026-09-21 上一轮凯奔服务器只读检查时，邮箱账户为 0、询价为 0、供应商报价为 0。该记录说明当时尚未接通此业务链；本轮没有重新连接服务器，实施与联调时需复查。

## 3. 首期范围与分工

| 主体 | 职责 |
|---|---|
| AI | 从原文提出结构化需求/报价草稿，说明候选供应商依据，起草邮件，解释各供货方案差异 |
| 系统 | 校验身份与单据归属，保存来源和版本，可靠执行邮件任务，计算逐行可比价格，记录真实状态 |
| 业务员 | 修订需求与回邮提取结果，确认收件人和邮件内容，确认正式报价记录，选择每行供货方案 |

首期默认规则：

1. 沿用当前单公司内部使用及 USD 正式报价范围。非 USD 回邮可保留原文和提取草稿，但不自动折汇、改币种或纳入 USD 排名；需补充供应商 USD 报价或等待后续多币种范围。
2. 邮件正文（纯文本/HTML 表格）为首期自动提取范围。附件保留原件和来源，支持人工查看后补录；PDF、Excel、扫描件自动解析/OCR 列为后续扩展，不把文件名当作可用证据。
3. 支持供应商只报部分件号、供货数量不足、暂不报价、同一件号多个供货方案及后续修订。未回复、明确无货、识别失败和待核实分别显示。
4. 报价关联使用需求行 ID。同一需求中出现两个相同件号时仍保留两行，不凭件号文字合并；替代件仅作为显式候选，确认适用后才参与该行的比较。
5. 发送询价、确认正式报价和标记中选保留人工确认。中选代表内部供货选择，不自动发送采购承诺、不直接生成订单，也不绕过现有报价审批与采购流程。
6. AI 不可用时可手工完成同一业务流程。旧客户端运行时保持关闭，本专项只增加所需服务端操作，不开发通用 Agent 平台。

## 4. 页面与操作流程

主流程：录入/提取需求 → 选择需求行及供应商 → 确认询价邮件 → 跟踪发送与回邮 → 核对报价草稿 → 逐行比价 → 确认供货方案 → 转入现有报价/采购入口。

建议沿用现有寻源路由，在一份 RFQ 的上下文中提供：

- 需求区：件号、数量、单位、成色要求、证书要求、需求日期、已有库存匹配与缺口。
- 供应商与发送区：候选依据、联系方式、询价内容预览，以及每家供应商的排队、发送、失败和回复状态。
- 比价区：以需求行为行、供应商为列；单元格展示可供数量、单价/币种、交期、成色/证书声明、有效期和来源。一个供应商有多个方案时可展开查看。
- 邮件核对面板：原邮件/附件与提取字段对照，支持纠正关联、补录、确认或忽略。
- AI 助手与时间线：本单建议、执行进度、待确认动作、人工修订和实际结果；刷新页面或切换入口后恢复。

供应商报价页继续用于全局查询并能跳回对应需求。Agent 工作台只作为任务汇总与入口，跳转至同一业务上下文；不能维护第二份寻源事实或单独计算“已完成”。

## 5. 开发待办

### SQ-01｜统一数据关联、状态与接口约定

- [ ] **待实施。** 在现有 RFQ/RfqLine、Inquiry/InquiryItem、SupplierQuote 上补齐发送记录、来源邮件/附件、提取草稿、修订及人工确认的关联；只新增无法由现有实体表达的数据。
- [ ] 定义发送状态、回邮处理状态、报价有效性和任务执行状态的独立含义。入队不算已发送，SMTP 接受不算对方已读，提取完成不算人工确认；需求完成度从各行派生。
- [ ] 定义重复提交、邮件去重、并发修订、旧单行记录兼容及权限规则，更新接口契约。未知历史关联保持待核实，不按相似件号自动补齐。

验收：同一需求从 AI 和人工入口读取到相同数据；跨 RFQ 行关联被拒绝；旧记录仍可读且不被错误关联。后续任务按此约定实施。

### SQ-02｜多件号需求与供应商候选进入统一上下文

- [ ] **待实施；依赖 SQ-01。** 复用多行录入和库存查询；将 AI 提取的 `items[]` 全部呈现，允许一次审核后建立一个 RFQ 多行，并保留原文来源与缺项。
- [ ] 逐行展示库存匹配与候选供应商。首期候选来自已录入的供应商档案、可核实历史记录及库存信息；缺少供货证据时提示待询价，不让 AI 合成库存或外部市场报价。
- [ ] 人工可调整供应商和各自询价范围；无有效联系方式、重复需求提交及已关闭需求给出明确处理结果。

验收：一份三件号需求完整保留三行，可选择不同供应商覆盖不同需求行；手工录入与 AI 提取进入同一发送流程，重复确认不重复建单。

### SQ-03｜真实询价发送与结果可核实

- [ ] **待实施；依赖 SQ-01、SQ-02。** 扩展现有询价发送接口，复用 OutboundEmail、Outbox 与 Worker；逐供应商生成独立邮件，确认收件人、需求行、主题、正文和发送版本后入队。
- [ ] 保存本次发送内容快照及稳定 Message-ID，并关联询价 ID/版本；各供应商互不暴露收件地址和他家报价。发送后的内容修改生成新版本，已排队任务不静默改内容。
- [ ] 展示发送失败、排队与可取消状态。处理重复点击、自动重试、权限撤销，以及 SMTP 已接受但结果未写回的情况；结果不确定时进入核实流程。

验收：对三家测试收件人各发送其应接收的需求行，逐封追溯至发送版本；刷新、双击不重复创建发送任务。稳定 Message-ID 不作为 SMTP“绝不重复投递”的保证；模拟崩溃时不会把不确定结果报为成功或盲目重发。

### SQ-04｜收件归档、线程匹配与附件原件

- [ ] **待实施；依赖 SQ-01、SQ-03。** 复用 IMAP 同步和去重，提取/使用 `In-Reply-To`、`References`、询价标识及已确认供应商联系人进行回复关联。
- [ ] 自动关联需依据一致；转发、修改主题、多询价混合、联系人不明或线索冲突进入待匹配队列，支持人工关联/拆分到多个需求行，保留操作记录。
- [ ] 附件通过现有文件存储保存原件与元数据、限定大小/类型并受权限控制；同步重试不重复落库。邮件正文、附件下载和业务引用遵守同一单据可见范围。

验收：普通回复正确归档；相同件号不同 RFQ 不串单；重复同步不重复创建邮件/附件/待处理记录；无法可靠匹配的邮件可人工完成归档。

### SQ-05｜AI 回邮提取与人工报价确认

- [ ] **待实施；依赖 SQ-01、SQ-04。** 新增供应商回邮提取能力，复用已有模型、发布提示词和调用日志；每个候选项保留原文依据、目标需求行、供应商、数量/单位、单价/币种、交期、有效期及原文提供的成色/证书/税运费条件。
- [ ] 缺失或歧义字段保持待核实。例如“2–3 weeks”保留区间与原文，“stock”不自行等同于当天可交付；不能无依据填数量、币种、证书或日期。
- [ ] 对照原邮件逐行修订和确认，确认后写入 SupplierQuote；同一封邮件可生成多个需求行的报价或同一行多个方案。人工修改保留，重解析生成候选版本而非覆盖已确认结果。
- [ ] 保留报价修订及来源；被客户报价或采购承诺引用的报价内容不可因新回邮被静默覆盖。识别失败、模型未配置和附件报价均支持手工补录。

验收：三件号回邮不漏行；缺项阻断需要这些信息的确认/排名；重复确认不重复生成报价；供应商更新价格后旧版及其业务引用仍可追溯。邮件内的指令文字不能改变系统权限或触发发送、选商动作。

### SQ-06｜逐需求行比价及中选一致性

- [ ] **待实施；依赖 SQ-01、SQ-05。** 同时修改比较和中选逻辑，以 `rfqLineId` 为范围；多行 RFQ 的旧整单比较入口也使用分组结果，避免保留一条混排路径。
- [ ] 先展示实际价格、可供量、交期及差异，再按兼容条件分组比较。USD、数量/单位、成色、证书声明、税运费口径和有效期缺失或不同的方案明确标注；未确认、过期、缺价及非 USD 草稿不参与可比最低价计算。
- [ ] 同一需求行可查看多家/多方案，首期人工确认一个当前中选方案；不同需求行互不影响。供货不足显示剩余缺口，不把整行标为满足；同一行多供应商数量组合自动优化后置。
- [ ] 中选与取消/更换采用事务及并发校验；选定新版不静默改写已引用旧版的客户报价或采购承诺。确定性计算负责排序，AI 解释来源与差异，缺少供应商绩效时仍可展示实际价格和交期。

验收：A 件号选供应商甲、B 件号选供应商乙可以同时保留；相同件号的两条需求行互不覆盖；并发选商不会产生冲突中选。不同件号/币种/条件不生成混合“最低价”，部分数量不显示全部满足。

### SQ-07｜AI 与人工共用的服务端任务执行

- [ ] **待实施；依赖 SQ-01，并逐步接入 SQ-02～SQ-06。** 仅为本专项建立服务端任务及所需的创建、确认具体版本、取消、重试接口；记录真实操作者、关联业务产物和执行结果。
- [ ] 发送、正式报价确认和中选调用与人工入口相同的业务命令。执行时重新检查当前权限、单据状态与版本，客户端/模型不得直接提交“已发送”“已中选”等事实。
- [ ] 支持等待回邮、等待人工、处理中、失败、取消、结果待核实与完成；复用 Worker 租约/幂等机制。人工修订或取消后，旧 AI 任务不能覆盖新结果。

验收：关闭浏览器后发送/收件仍运行，重新打开可恢复进度；Worker 重启及双端确认不重复产生业务记录；任务成功能追到真实产物。不得通过重新开启旧 `AGENT_RUNTIME_EXECUTION_ENABLED` 来代替此项。

### SQ-08｜统一 AI 寻源工作台与现有下游衔接

- [ ] **待实施；页面框架依赖 SQ-01，完整交付依赖 SQ-02～SQ-07。** 按第 4 节整合现有寻源、报价列表和邮件核对组件，提供单件号和多件号同一套操作，保留原有入口的跳转兼容。
- [ ] 展示每行待报价数、待确认数、可比方案、数量缺口，以及每家供应商的发送/回复状态；AI 建议、人工动作与真实执行结果在一条时间线上呈现。
- [ ] 选中供货方案后带入既有客户报价/采购相关入口，保留行 ID 与报价版本来源；继续遵守原有金额、有效期、审批及采购约束。

验收：业务员从一份需求完成发送、核对回邮、逐行比价和下游入口交接，无需重复录入件号/价格；页面刷新、从 Agent/供应商报价入口进入时状态一致。未实现操作不显示为成功。

### SQ-09｜贯穿业务验收与发布

- [ ] **待实施；按下列里程碑验收。** 使用隔离 PostgreSQL、受控邮件服务与模型协议替身验证完整链路；相关新增迁移、权限、幂等、并发、行归属及页面流程纳入现有检查。
- [ ] 取得指定测试邮箱与可用模型配置后，验证真实 SMTP 发出、真实 IMAP 回邮、模型提取、人工确认及逐行比价；真实业务收件人范围由业务负责人明确。凭据通过既有配置入口提供，不写入本文或仓库。
- [ ] 通过既有 CI、Build and Verify、备份及发布流程后再部署；核验 Worker、迁移、存量数据对账和该专项真实邮件样本，记录版本、结果与剩余限制。

验收：下表核心用例有对应证据；分别说明本地检查、真实服务联调和部署检查。缺少邮箱/模型凭据仅阻塞相应真实联调，开发和隔离验证继续；不得据模拟结果宣称真实邮件链路已上线可用。

## 6. 实施顺序与里程碑

| 里程碑 | 交付范围 | 完成条件 |
|---|---|---|
| M0：接口与页面范围明确 | SQ-01；SQ-08 页面结构约定 | 行关联、状态、版本、权限、人工确认边界和首期样本已明确 |
| M1：业务闭环 | SQ-02 的手工入口、SQ-03、SQ-04、SQ-05 的手工确认、SQ-06、SQ-08 对应界面 | 隔离环境完成多件号、多供应商发信/回邮/确认/比价；不依赖 AI 也能处理异常 |
| M2：AI 寻源接通 | SQ-02/SQ-05 的 AI 能力、SQ-07、SQ-08 完整联动 | AI 减少重复录入，人工修订受保护，任务可恢复且不重复执行业务动作 |
| M3：凯奔环境可用 | SQ-09 | 指定真实邮箱与模型联调通过，候选 CI/发布检查通过，服务器实际流程验收完成 |

SQ-07 的接口和执行边界在 M0 约定，随各业务命令逐段实现；不等到页面完成后再新增一套 AI 流程。M1 是中间交付，完成本专项需达到 M3。

## 7. 核心验收样本

| 编号 | 场景 | 预期结果 |
|---|---|---|
| A01 | 一个件号、三家供应商分别报价 | 三份来源可追溯，按该需求行比较价格与交期 |
| A02 | 三个件号、三家供应商，回复范围各不相同 | 每行独立汇总；可分别中选；未报价行仍显示缺口 |
| A03 | 两个 RFQ 使用相同件号；一份 RFQ 内也有重复件号 | 依照业务/行 ID 关联，无法确定时待匹配，不猜归属 |
| A04 | 邮件双次同步、用户双击确认、Worker 重启 | 不重复建需求/报价/发送任务；SMTP 结果不确定时待核实 |
| A05 | 缺币种、非 USD、交期区间、缺数量、仅附件报价 | 原文保留，缺项可补录，不伪造确定值或强行排名 |
| A06 | 供应商修订报价、旧版已被下游引用 | 新旧版均可追溯，引用不变；过期方案不能继续中选 |
| A07 | 人工修订与 AI 重解析/旧任务同时发生 | 人工已确认内容不被覆盖，旧版本确认被拒绝 |
| A08 | 跨归属访问、两个操作者同时选商 | 拒绝越权，同一行中选一致，其他行不受影响 |
| A09 | 模型超时或缺配置、邮箱发送失败、无回复 | 原因与下一动作清晰；手工路径可继续，不显示虚假成功 |
| A10 | 指定真实邮箱完整发出与回复，模型参与提取 | 实际收到测试询价，回邮进入对应需求，确认报价后矩阵数据正确 |

每个样本随相关功能一次性验证并复用证据；实现、输入或相关风险变化后才重跑。真实业务试用记录人工处理耗时和修正次数，用于评估 AI 是否减负，不预设缺乏基线的节省比例。

## 8. 后续范围与实施输入

本期后置：公开供应商门户、外部平台自动抓取、全格式附件/OCR 自动提取、自动汇率/税运费归一、多供应商数量组合优化、自动催价外发，以及无需人工确认的采购/订单动作。

真实联调前准备指定收发邮箱、三家受控收件人/供应商映射，以及包含单行、多行、部分报价和修订的脱敏邮件样本；在现有模型管理中配置可用模型。测试开始前复查服务器当前邮箱及版本状态。

交付记录随对应 `SQ-*` 更新为已完成，并附实现文件、验证证据和未验证部分。

## 9. 开发进展记录

### 2026-09-22｜首个本地切片

已完成：

- SQ-01/SQ-03 部分：`OutboundEmail` 可关联 `Inquiry`；询价发送接口复用 Outbox 和独立 Worker。接口返回 `202` 只表示排队，Worker 记录 SMTP 接受后才将询价更新为已发送；列表和详情区分草稿、排队、发送和失败。
- SQ-06 核心行隔离：比价优先使用 `rfqLineId`/`inquiryItemId`，旧 RFQ/询价标识只在可唯一解析需求行时兼容；多行混排被拒绝。中选清除与设置按需求行在 Serializable 事务中完成，不再覆盖其他需求行。
- SQ-08 页面切片：沿用现有寻源页面，加入逐需求行的询价进度、邮件草稿核对与发送，以及实际价格、可供数量、交期、有效期和当前中选状态展示；没有新建第二套寻源页面或事实数据。
- 接口契约：询价发送状态、可选邮件内容、逐行比价范围及响应已写入 OpenAPI，并重新生成前端类型。

本地验证：

- 服务端相关 58 项测试通过，覆盖发送入队/幂等/错误脱敏/Worker 回写，以及逐行比较、来源冲突和行内中选；服务端 TypeScript build 通过。
- OpenAPI 契约测试 23 项通过；严格路由覆盖、契约 lint 和生成类型一致性检查通过（保留两项与本次无关的既有“缺少 2XX 响应”警告）。
- 前端寻源工作台 3 项定向测试通过，完整 TypeScript/Vite build 通过；相关页面、API 客户端和 hooks 的 ESLint 检查通过。

尚未完成：SQ-04 收件线程匹配与附件原件、SQ-05 AI 回邮提取/人工确认、SQ-07 服务端可信 AI 任务，以及 SQ-09 真实 SMTP/IMAP、模型、迁移和服务器部署验证。当前比价仍需继续补齐条件兼容、过期/缺项参与规则和数量缺口计算；失败投递目前通过状态提示人工处理，尚未提供寻源页内的取消/重试命令。

### 2026-09-22｜第二个本地切片

已完成：

- SQ-04 核心闭环：收件保存规范化 `In-Reply-To`/`References`，仅在唯一已发邮件 Message-ID 与询价供应商邮箱同时一致时自动关联；冲突、未知联系人和无匹配分别进入待核状态。人工关联支持保留原因，不按主题或件号猜单。
- SQ-04 附件原件：正文与安全附件一并归档到现有对象存储，保存哈希、大小、类型和邮件关系；邮件权限同时约束附件下载。危险或超限附件保留拒绝/部分保存状态且不阻塞 IMAP 游标；数据库失败时补偿清理已上传对象。
- SQ-05 草稿与确认：新增邮件来源报价草稿、乐观锁版本、逐项稳定键及正式报价来源关系。一封邮件可确认多条需求行或同一行多个方案；确认事务只接受明确需求项、完整 USD 价格/数量和单一交期，重复确认复用同一批正式报价 ID。
- SQ-05 AI 提取：新增发布版 `supplier_quote_extraction` 内置智能体。邮件内容按不可信输入处理，严格 JSON 输出，原文依据必须能在主题/正文中定位；模型只创建稀疏草稿，不能直接创建正式报价。缺模型或识别失败时仍可创建手工草稿。
- SQ-08 页面切片：现有寻源工作台增加回邮核对弹窗，展示匹配状态、正文、受控附件及附件拒绝/部分保存原因，支持 AI/手工建稿、逐项修订、保存和显式确认；刷新后按邮件与询价恢复最新草稿，确认成功后才刷新逐行比价。
- 接口契约：邮件关联、附件状态、报价草稿创建/恢复/提取/修订/确认已写入 OpenAPI 并重新生成前端类型。

本地验证：

- 服务端本轮及既有寻源相关 13 个测试文件、115 项测试通过；刷新恢复增量测试后相关 2 个文件、18 项再次通过；服务端 TypeScript build 通过。
- 前端回邮核对与 API 客户端 2 个测试文件、13 项测试通过，OpenAPI 契约另有 24 项通过；完整 TypeScript/Vite build 和相关 ESLint 检查通过。
- PostgreSQL 与 SQLite Prisma schema 校验通过；OpenAPI 严格覆盖 370 个操作、61 个 mount，契约 lint 与生成类型一致性检查通过，仍仅有两项与本专项无关的既有“缺少 2XX 响应”警告。

尚未完成：迁移尚未在真实 PostgreSQL 执行，也未使用真实 SMTP/IMAP、对象存储或模型服务联调。SQ-04 的全邮箱待匹配操作入口、SQ-06 的条件/有效期/数量缺口完整可比规则、SQ-07 可恢复服务端 AI 任务和 SQ-09 凯奔服务器部署验收仍待后续切片。

### 2026-09-22｜第三个本地切片

已完成：

- SQ-04 待匹配队列：邮件列表支持只查询 `UNMATCHED`/`NEEDS_REVIEW` 且没有已确认询价关联的回邮；同步中的邮件和已人工/自动确认关联的邮件不会进入队列。现有寻源工作台直接展示全邮箱待匹配回邮、匹配原因和附件状态，人工选择询价后完成关联；邮箱不一致时必须填写原因。
- SQ-06 可比资格：逐行比价保留所有原始报价，但只有未过期、币种已核验为 USD、状态可用的正式报价参与最低价、价格区间和规则排序。每份报价返回排除原因与缺项提示，过期报价不能中选。
- SQ-06 数量与条款：按当前需求行返回单份报价的可供数量、是否覆盖、数量缺口，以及可比报价数、过期数、最大单份可供量和剩余缺口；部分供货方案仍可人工保留，但不会显示为全部满足。来自确认草稿的成色和证书声明按稳定项键精确回溯，无法可靠取得时明确显示未知。
- SQ-07 可信任务最小闭环：新增服务端归属的 `SourcingAiTask`，持久化操作者、邮件、询价、状态、尝试次数、幂等键、安全错误摘要和产物草稿。创建前先落任务记录，条件更新认领执行；失败可在同一任务上重试，待处理/失败任务可取消，任务归属仅本人或管理员可见。任务只能创建可编辑草稿，不存在正式报价写路径。
- SQ-08 页面联动：页面的 AI 提取按钮已切换到可信任务接口；失败时展示尝试次数、安全原因以及重试/取消和手工草稿入口。比价表显示可比/排除、过期、数量不足、缺口、成色和证书提示，且不在前端自行重算最低价或规则分。
- 接口契约：待匹配查询、可信任务创建/读取/重试/取消，以及新增比价资格、数量和条款字段均进入 OpenAPI；生成类型已同步。

本地验证：

- 服务端寻源相关 14 个测试文件、132 项测试通过，覆盖待匹配过滤、逐行比价资格、过期与数量缺口、可信任务归属/幂等/失败/重试/取消、草稿确认及不生成正式报价；服务端 TypeScript build 通过。
- 前端页面、API 客户端和 OpenAPI 契约 3 个测试文件、43 项测试通过；完整 TypeScript/Vite build 和本切片相关 ESLint 检查通过。
- PostgreSQL 与 SQLite Prisma schema 校验通过；OpenAPI 严格覆盖 375 个操作、62 个 mount，契约 lint 与生成类型一致性检查通过，仍仅有两项与本专项无关的既有“缺少 2XX 响应”警告；`git diff --check` 通过。

尚未完成：三批迁移尚未在真实 PostgreSQL 执行，也未使用真实 SMTP/IMAP、对象存储或模型服务联调。SQ-06 仍需补齐税费/运费口径和更完整的兼容条件分组，多供应商数量组合优化保持后置；SQ-07 当前采用同步执行并支持失败重试，尚无后台 Worker 和运行中协作取消；SQ-09 凯奔服务器部署验收仍待后续切片。

### 2026-09-22｜第四个本地切片

已完成：

- SQ-05 商务条款提取：报价草稿和内置 AI 提取结构增加含税、含运费和 Incoterm。AI 只能保留邮件明确表达的内容，缺失值保持未知，不依据 Incoterm 反推税费或运费；Incoterm 在校验后统一为大写。手工草稿、修订、可信任务产物和正式报价的来源草稿均保留这些字段。
- SQ-06 同口径比价：比较算法升级为 `supplier-quote-rule-v3`，在每个实际件号内继续按成色、证书、含税、含运费和 Incoterm 划分商务口径组。最低价、价差和规则分只在同一口径组内计算；同一件号存在多个有效口径组时，不再返回跨口径价格区间或全局第一名。未知税费/运费/Incoterm 明确返回提示。
- SQ-08 页面与接口契约：回邮核对面板可人工选择含税/未税、含运费/不含运费并填写 Incoterm；逐行比价展示这些条款和商务口径。OpenAPI 增加草稿字段、报价口径键/标签和口径分组结构，前端生成类型已同步。
- SQ-09 上线准备审计：只读核对本地凯奔服务器资料与仓库部署文件，没有读取凭据、连接服务器或数据库。资料仍将 `101.43.50.30` 记为 SQLite Compose，而现有标准发布链路和邮件 Outbox/IMAP 运行载体要求 PostgreSQL 与独立 Worker；另一台 `124.223.196.165` 的 PostgreSQL/Worker 验收记录属于测试机，不能替代凯奔现状。

本地验证：

- 服务端寻源相关 14 个测试文件、139 项测试通过，覆盖 AI 商务条款校验与未知值、草稿修订/确认字段保留，以及不同税费/运费/Incoterm 不混合排名；服务端 TypeScript build 通过。
- 前端页面、API 客户端和 OpenAPI 契约 3 个测试文件、43 项测试通过；完整 TypeScript/Vite build 通过。
- OpenAPI 严格覆盖 375 个操作、62 个 mount，契约 lint 与生成类型一致性检查通过，仍仅有两项与本专项无关的既有“缺少 2XX 响应”警告；本切片相关 ESLint 与 `git diff --check` 在收口时检查。

尚未完成：尚未确认凯奔生产目标应继续使用旧 SQLite profile，还是升级到 PostgreSQL + Worker；在该部署拓扑确定前不执行远端发布。三批迁移仍未在真实 PostgreSQL 执行，真实 SMTP/IMAP、附件存储和模型服务也未联调。SQ-06 的多供应商数量组合优化保持后置；SQ-07 的 AI 提取任务仍为请求内同步执行，尚无后台 Worker 和运行中协作取消。

### 2026-09-22｜第五个本地切片

已完成：

- SQ-07 后台执行：创建 AI 回邮提取任务的 HTTP 接口只持久化 `PENDING` 并立即返回，不再在请求内等待模型。独立 Worker 定时认领任务，模型成功后只创建可编辑报价草稿；失败仅保存脱敏摘要。失败重试在原任务上原子地重新排队并递增尝试次数。
- SQ-07 恢复与取消：Worker 使用 `RUNNING + startedAt` 作为执行令牌，超时任务可在最大尝试次数内恢复；取消、超时重领或新执行令牌产生后，旧模型响应即使返回也不能提交草稿。`PENDING`、`RUNNING` 和 `FAILED` 均可取消，Worker 停止时将 AI 任务纳入在途任务等待。
- SQ-08 页面恢复：回邮核对面板提交任务后展示排队/运行状态并轮询服务端，完成后自动载入草稿；关闭再打开时按邮件与询价精确恢复最近任务，不依赖浏览器内存。任务查询继续受操作者/管理员权限范围约束。
- 接口契约与运行配置：任务查询增加成对的 `emailId`/`inquiryId` 过滤；创建、重试和取消说明更新为后台语义。Worker 支持 `WORKER_SOURCING_AI_TASK_INTERVAL_MS` 和 `WORKER_SOURCING_AI_TASK_ENABLED`，API 的 Socket-only 内联 Worker 不消费 AI 任务。

本地验证：

- 服务端寻源与 Worker 相关 16 个测试文件、148 项测试通过，覆盖只入队、幂等、精确恢复、失败重排、运行中取消、超时恢复、最大尝试次数、旧执行令牌拒绝和 Worker 优雅停止；服务端 TypeScript build 通过。
- 前端页面、API 客户端和 OpenAPI 契约 3 个测试文件、45 项测试通过，覆盖排队轮询、完成后自动载入和重新打开后的任务恢复；完整 TypeScript/Vite build 通过。
- OpenAPI 严格覆盖 375 个操作、62 个 mount，契约 lint 与生成类型一致性检查通过，仍仅有两项与本专项无关的既有“缺少 2XX 响应”警告；相关 ESLint 与 `git diff --check` 通过。

尚未完成：凯奔目标部署拓扑仍需确认；若继续使用当前记录中的 SQLite Compose，则没有独立 Worker 运行载体，不能满足邮件与 AI 后台任务。三批迁移和本切片 Worker 仍未在真实环境验证，真实 SMTP/IMAP、附件存储和模型服务联调也未完成。多供应商数量组合优化及公开市场等后置范围不在本切片内。
