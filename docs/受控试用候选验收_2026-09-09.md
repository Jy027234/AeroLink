# 受控试用候选验收

本记录服务于原计划 M1 的少量员工受控试用，不替代 M2/M3 的完整自有库存和背靠背验收。D00–D18 总体目标保留；D15、D16、D18 不作为本次候选前提，D17 只纳入必要入口。当前尚未推送或部署，不是用户试用放行记录。

## 候选隔离与已完成修复

- 独立目录：`C:/Users/jiaxi/AppData/Local/Temp/aerolink-trial-candidate-20260909`，分支 `codex/trial-candidate-20260909`。
- 首次候选：`369dbb02a5efc342d9776598f1104bdaab0afe1e`。以下结果仅对应这个提交，依赖更新后的候选需要另行记录。
- 独立依赖目录通过 Node 22.23.2 / npm 10 干净安装；没有使用原工作区的生成客户端或未提交代码。
- 候选包含 35 条迁移。原工作区的供应商直发 schema / 011 SQL 仍未部署、未集成命令和验收，未进入此候选。
- 证书撤销和旧续期接口已补 `certificate.issue` 能力门槛；撤销使用严格输入、当前操作者及 Serializable/CAS 更新。无新证据的日期续期已关闭，页面不再提供该操作。
- Build and Verify 的 Node 版本从浮动 22 改为 22.23.2，检查同时覆盖 CI 与发布构建工作流；迁移数量以候选源码动态核对。

## 首次候选的本地证据

| 检查 | 结果 | 证据路径（本机 TEMP） |
|---|---|---|
| 后端构建及测试 | 142 文件 / 780 项通过 | `aerolink-trial-backend-build-20260909.log`、`aerolink-trial-backend-tests-20260909.log` |
| 前端构建及测试 | 22 文件 / 80 项通过；构建通过 | `aerolink-trial-frontend-build-20260909.log`、`aerolink-trial-20260909/frontend-vitest.log` |
| API、模块、功能边界及 CI 静态检查 | 通过；347 operations / 58 mounts / 0 inventory-only | `aerolink-trial-20260909/results.txt` 及同目录日志 |
| 证书真实认证 HTTP | 当前角色、旧 token 撤销、禁止续期、严格输入、并发撤销单赢家、原文件/有效期/追溯保留均通过 | `aerolink-trial-cert-http-20260909.log`；脚本 `server/src/scripts/checkCertificateMutationHttp.ts` |
| PostgreSQL | `aerolink_trial_candidate_20260909` 是收货合成库的独立克隆；35 迁移一致，datamodel diff 为空 | Prisma migrate status / diff 现场输出 |
| 全量浏览器回归 | 首次 57 通过 / 3 失败，失败均为 PDF 运行环境未指定现有浏览器 | `aerolink-trial-e2e-20260909.log` |
| PDF 环境修正后的复验 | 三个相关文件全部 11 项通过，包括原先失败的报价、多行、商业修订及合同下载 | `aerolink-trial-pdf-recheck-20260909.log` |
| 浏览器交易后的数据对账 | 库存、金额、JSON 配置、交易状态四类均通过 | `aerolink-trial-check*Reconciliation-20260909.log` |

浏览器测试库 `aerolink_trial_e2e_20260909` 是新建空库，应用全部 35 迁移后仅写入合成种子。测试使用端口 3109 / 5199，不复用原开发 API。首次安装显式跳过 Puppeteer 浏览器下载，默认 Chrome 152 不存在；设置 `PUPPETEER_EXECUTABLE_PATH` 指向现有 Chrome 148 后，实际 PDF 探针及上述 11 项复验通过。该配置仅证明本机渲染，不代替部署镜像中的 Chromium 验证。所有测试进程由 Playwright 结束，没有留下本轮 API/Worker 服务供用户试用。

OpenAPI 保留 3 个关闭接口没有 2xx 的警告，不伪造成功响应来消除警告。依赖安装时的 `--no-audit` 只用于功能安装，发布审计在下表独立执行，不能据安装成功认定审计通过。

## 依赖修复后的候选

程序源码提交：`b8a66577c594aaea27e29dac43796ee0e5282f5c`。独立目录默认 `npm ci` 两端成功，没有使用 `--legacy-peer-deps`。本节记录完成后仅提交验收文档；实际远程 CI 和发布 manifest 仍须对应届时最终候选的完整 SHA。

| 检查 | 结果 | 本机 TEMP 证据 |
|---|---|---|
| 后端构建与全套测试 | 142 文件 / 780 项通过；Vitest 4.1.11 | `aerolink-trial-fixed-backend-build-20260909.log`、`aerolink-trial-fixed-backend-tests-20260909.log` |
| 前端构建与全套测试 | 22 文件 / 80 项通过 | `aerolink-trial-fixed-frontend-build-20260909.log`、`aerolink-trial-fixed-frontend-tests-20260909.log` |
| 在线依赖审计 | 前后端生产/开发四层均 0 漏洞、exit 0 | `aerolink-trial-audit-fixed-20260909` 中四份 `.summary.json` 与原始报告 |
| 完整浏览器回归 | 60/60 通过，4.6 分钟，retries=0；包括原先 PDF 失败的流程 | `aerolink-trial-fixed-e2e-20260909.log` |
| 浏览器交易后的数据对账 | 库存、金额、JSON 配置、交易状态均通过，0 阻断 | `aerolink-trial-fixed-check*Reconciliation-20260909.log` |

最终浏览器验证使用全新 `aerolink_trial_e2e_fixed_20260909` 合成库，35 条迁移从空库应用，未删除或重用先前验收库。固定 PDF 浏览器环境同上。运行后检查 3109 / 5199 均无监听，候选工作树干净；没有遗留试用服务或向真实客户发送内容。

首次依赖失败报告保留于 `aerolink-trial-audit-20260909`。两端 Vitest 锁定 4.1.11，后端 Morgan 1.12.0、Multer 2.3.0、Nodemailer 9.1.1。npm 10 的 Vitest 锁生成曾出现 Arborist `edgesOut` 异常，在临时目录生成锁后经默认 peer 模式检查及上述真实 `npm ci` 验证；候选安装和 CI 不携带临时生成时的 legacy peer 参数。

## 当前未放行项

1. **Docker 镜像构建证据缺失。** 本轮重试拉取 `node:22.23.2-bookworm-slim` 与 `node:22.23.2-alpine`，仍因 Docker Hub TLS handshake timeout 失败；没有关闭 TLS 校验或替换成未经验证的镜像。Windows 主机直连诊断另返回证书吊销服务不可达，也未关闭吊销检查。
2. **同一候选 SHA 的远程 CI、SBOM、release manifest 与目标测试环境部署证据缺失。** 尚未推送、未操作云端环境，不以本地日志代替远程 release gate。
3. **真实岗位试用与历史数据恢复验收未完成。** 本记录仅使用合成数据；若投入历史业务数据，必须先满足 D09 的真实备份、附件和对账恢复要求。完整背靠背仍需 D14 直发、收付款凭证及相应岗位页面。

本次依赖修复的上游依据：[Multer 2.3.0](https://github.com/expressjs/multer/security/advisories/GHSA-wc9g-mqfw-jrwm)、[Nodemailer 9.1.1](https://github.com/nodemailer/nodemailer/security/advisories/GHSA-8m3c-c648-2xjj)、[Morgan 1.12.0](https://github.com/expressjs/morgan/security/advisories/GHSA-jxfw-x594-9x9m)、[Vitest 4.1.11](https://github.com/vitest-dev/vitest/security/advisories/GHSA-82fw-gwwq-j7x9)。修复版本已由 npm 官方 registry 查询确认；最终以锁文件、在线审计和实际回归共同验收。
