# D01 远程候选验证（2026-09-11）

用户已明确授权推送候选、运行验证，并在指定测试服务器执行部署。草稿 PR 保持未合并。

## 最新部署进展

**最终结果：**修复候选 `af1b1f8` 的 CI / Release Gate / Build 全部成功，已正式部署并完成 Worker 重建后的复验。可以开始员工岗位试用，入口及完整证据见 [测试服务器试用发布结果](测试服务器试用发布结果_2026-09-11.md)。下面保留故障发现和修复阶段的记录。

`681c6a5` 已部署到指定测试服务器，数据库由 17 条迁移升级到 41 条。发布脚本完成数据库备份、健康、镜像及四类对账后，人工追加检查发现 Worker 因生产 Compose 缺少 JWT 环境变量而重启；原验证仅核对镜像 ID，遗漏运行稳定性。

已通过服务器内现有环境变量恢复 Worker，未生成新密钥；原发布记录状态改为 `worker_configuration_recovery`，保留这次验证遗漏。临时只读账号的实际 HTTP 登录、身份、能力、刷新、退出及退出后令牌拒绝验证通过，账号随后删除，现有员工账号保留。该轮截图仅显示页面加载中，不能据此认定工作台内容验证通过。

正式修复候选为 `af1b1f8f4c10ca16ab35eefcd8e4de66abefff25`：生产 Worker 获得必需 JWT 配置，发布验证增加默认 5 秒的运行/重启次数检查，稳定窗口限定 2–30 秒。稳定 Worker 通过、重启状态拒绝、重启计数增长拒绝及禁用窗口拒绝的脚本契约均通过。测试服务器已按新候选构建镜像，下一步等待该候选 CI / Build 成功凭证后重新发布，移除对临时覆盖配置的依赖。

以下章节记录 `681c6a5` 的远程基线及部署前准备，所述“尚未切换”仅表示该阶段状态；最新部署结果以上方及后续最终发布记录为准。

## 当前候选与 PR

- 草稿 PR：[内部交易审批、质量履约及采购结算流程 #2](https://github.com/Jy027234/AeroLink/pull/2)。
- 远端分支：`codex/complete-trial-candidate-20260910`。
- 当前候选：`681c6a556fee945d335e34a2963019e9fab20e18`。
- 原始业务验收候选 `a4cc489` 保留不变；新候选只增加一处 Docker 构建源修复和两处 E2E 岗位会话地址修复，未改业务实现或迁移。
- 新候选本地目录：`C:/Users/jiaxi/AppData/Local/Temp/aerolink-trial-release-fix-20260911`，本地分支 `codex/trial-release-apt-bootstrap-20260911`。主开发分支已同步两处修复。

最初 HTTPS 推送被 GitHub 拒绝，原因是已存 OAuth 身份缺少 workflow scope。随后使用本机已经配置且具有访问权限的 SSH 身份成功推送；没有移除工作流、放宽仓库权限或更改全局认证配置。

## 远程验证发现与修复

1. `a4cc489` 的 [Build and Verify 34500761750](https://github.com/Jy027234/AeroLink/actions/runs/34500761750) 在安装 CA 前遇到默认 Tencent APT 源的 HTTPS 重定向，证书验证失败。`a6b1e99` 将默认源改为 Debian 官方 HTTP，保留 Debian 签名元数据及包校验；[34501285443](https://github.com/Jy027234/AeroLink/actions/runs/34501285443) 随后全部通过。
2. `a4cc489` 与 `a6b1e99` 的完整 CI 均发现两个 E2E 独立账号上下文错误连接本机调试端口 5220/5222。`681c6a5` 将它们改为使用 Playwright 的 baseURL fixture，保持独立上下文、岗位授权及业务断言。本地在非默认前端端口且不设置 PLAYWRIGHT_BASE_URL 时，两个受影响流程通过。
3. 本地同时运行的另一个未修改用例在 Vite 页面“加载中”状态超过 30 秒；保留该失败，串行定向复验随后 1/1 通过，未增加超时或重试次数。

本地日志：`C:/Users/jiaxi/AppData/Local/Temp/aerolink-role-context-port-fix-20260911.log` 与 `aerolink-role-context-manager-recheck-20260911.log`。前者 2 通过/1 失败，后者 1 通过，不写成全套一次通过。

## 当前远程证据

- [Build and Verify 34502283283](https://github.com/Jy027234/AeroLink/actions/runs/34502283283)：成功，headSha 与当前候选一致。前后端构建、Prisma、两种 Compose、后端/HTTP 网页/TLS 网页镜像、SBOM、manifest、发布脚本语法与契约均通过。
- artifact `aerolink-sbom-681c6a556fee945d335e34a2963019e9fab20e18`，ID `10162480737`。已下载真实 artifact，manifest 的 SOURCE_REF/RELEASE_TAG 均为当前候选，CI_RUN_URL 指向上述运行；未伪造发布凭证。
- [完整 CI 34502266912](https://github.com/Jy027234/AeroLink/actions/runs/34502266912)：全部成功，包含 Release Gate。后端 152 文件/862 项、前端 27 文件/100 项通过；导航浏览器 4 项、报价合同浏览器 11 项通过；完整浏览器 60 通过、2 个显式开关用例跳过。两个跳过项由既有专用采购/结算流程验收覆盖，不写成 62 项全部执行。
- Secret Scan 的全历史/工作区扫描通过；已下载并核对四份审计摘要，exitCode 均为 0、漏洞总数均为 0。最终 Release Gate artifact ID `10162660522`，名称 `ci-release-gate-2f1217d6742c147827bdf46fae1cdc2f747b1802`，记录所有必需作业 success、四层 audit pass。
- PR 临时合并提交为 `2f1217d6742c147827bdf46fae1cdc2f747b1802`，其源码树与候选均为 `389117649ee621952b0d6df15589d68d6fc1d085`。CI artifact 使用临时合并 SHA，发布 manifest 使用候选 SHA，通过源码树一致性关联两者。

本机发布凭证目录：`C:/Users/jiaxi/AppData/Local/Temp/aerolink-release-evidence-681c6a5-20260911`。

完整 CI 日志为 `C:/Users/jiaxi/AppData/Local/Temp/aerolink-ci-34502266912.log`，审计和门禁文件在 `C:/Users/jiaxi/AppData/Local/Temp/aerolink-ci-evidence-681c6a5-20260911`。发布凭证已同步到测试服务器独立目录的 `sbom/`，内容来自成功工作流。

## 测试服务器候选产物

源码归档 SHA-256：`50baf3259dcf9bd9bc60e8edbab841b1809d315da3337044433de10a30833ebc`，上传后在服务器校验通过。独立目录：`/home/ubuntu/aerolink-trial-681c6a5-20260911`。

服务器上的默认 Debian 下载约 24.8 kB/s；此前 `a6b1e99` 构建在出现新候选后被明确终止，未把观察超时当作构建失败。新候选通过已有 APT_MIRROR 参数选择该服务器可达的 Tencent HTTP 源，复用已通过的签名包层；没有关闭 TLS 或 APT 校验。GitHub 则从默认官方源完成了实际构建。

| 镜像 | 验证结果 |
|---|---|
| `aerolink-trial-backend:681c6a556fee945d335e34a2963019e9fab20e18` | 构建通过，ID `sha256:3fc48d6a21b2133001585db166d4ed1e251de180d781e5ebc6840a0e26a2554b` |
| `aerolink-trial-web:681c6a556fee945d335e34a2963019e9fab20e18` | 构建通过，ID `sha256:897d46ffb4ef26a14b03c871e1a331804bdb542cae68baace26047b382e763fb` |

两个镜像 ID 与上一轮实际运行时检查的镜像一致，因此已有 Node 22、PDF 生成及 Nginx 配置证据仍指向相同不可变镜像。新版本的应用服务尚未启动或切换。

候选 source 目录已在服务器内复制现有环境配置及加密 secret，文件权限为 600，未输出或下载这些凭据；仅在候选配置中开启 REQUIRE_ACTIVE_ADMIN=true，现有运行配置未修改。实际 Compose 配置校验通过。新镜像也已建立 `aerolink-prod-backend:<候选SHA>` / `aerolink-prod-web:<候选SHA>` 引用，尚未更改 latest 或运行容器。

## 待执行的服务切换

保留当前 80 端口与测试访问地址，按现有发布脚本备份数据库、应用 41 条迁移、切换 API/Worker/网页并执行健康、镜像 ID、迁移及四类对账。现有数据库的隔离恢复和 17→41 升级预演已经通过，详见 [服务器预检](测试服务器发布预检_2026-09-10.md)。真实员工岗位走单仍需在切换之后完成。

当前旧版 API/Worker 镜像 ID 为 `sha256:d6779cb74a6eb70dca7eecbef5ec5a404f098b12ed53cb8c34875a96ab536189`，网页为 `sha256:aef5c585b36273357feeb2a009ad519bec94c0171e10acba8941eef0fbf1c20c`。发布前需将实际旧镜像建立为发布脚本所用的 latest/rollback 引用，不能因旧部署仅有固定标签而丢失回滚依据。若现场镜像已变化，先重新核对，不覆盖新状态。

镜像回滚不会自动还原数据库。数据库恢复仍按备份记录单独执行，不把镜像回滚描述为全数据回滚。本轮没有合并 main、改动运行库、发送业务邮件或切换现有服务。
