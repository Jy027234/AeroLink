# D14 结算凭证与冲销验收

2026-09-10。本增量服务于用户已确认的 USD 自有库存与背靠背交易范围。后端结算、财务登记页面、销售应收视图和真实凭证上传下载均已通过本地验证。随后完成采购至交付、结算的同一订单样本，见[完整交易岗位验收](D14_完整交易岗位验收_2026-09-10.md)。目标环境发布及真实员工验收仍待完成，不据此宣称已可供员工试用。

## 已实现的业务边界

- 现代销售订单建立一份应收记录；每份已确认或关闭的采购承诺建立一份应付记录。初始金额由来源交易的 Decimal 金额生成，调用方不能修改币种、基础金额、操作者或来源快照。
- OPEN、收付款 PAYMENT、信用冲减 CREDIT、退款 REFUND、全额冲销 REVERSAL、账期调整 TERMS 均追加不可修改的历史记录。每次登记需要外部系统、凭证号、凭证行、原因、实际发生时间和新上传附件。
- 外部凭证三元组全局唯一；多个分摊使用不同凭证行。系统只登记已有外部凭证，不发起银行付款，也不实现总账、税务或汇兑。
- 已成立订单的结算不依赖原报价现在是否过期。历史基础金额与交易对手快照保留；后续商业调整通过有凭证的冲减和冲销记录反映。
- 余额区分原始应收/应付、累计收付款、退款、有效收付款、信用冲减、调整后应收/应付、未付、超付、待退款。退款不得超过有效收付款，信用冲减不得超过原始金额，冲销必须对应同账户尚未冲销的原始记录。
- FINANCE 按现有策略登记财务事实；销售仅查看授权订单的应收记录。应付与其附件要求当前成本权限。旧 `/uploads` 链接不能绕过结算附件权限。
- 附件登记时绑定精确版本与 SHA-256。历史附件撤销后不能下载，但不抹掉已经登记的现金事实，也不阻断使用新凭证追加更正。
- 命令要求 Idempotency-Key，缓存仅保留资源 ID，重放前后重新检查当前权限并重新读取金额；Serializable 事务、版本 CAS 和提交前延迟约束检查共同保护并发登记。

接口为 `GET/POST /api/settlements`、`GET /api/settlements/:id`、`POST /api/settlements/:id/records`。列表要求明确的 orderId。

## 数据库与验收证据

独立库 `aerolink_settlement_test_20260910` 从既有直发合成库克隆，地址限制为本机 55970；未修改原直发库、收货库、试用候选库或真实业务库。新迁移 `20260909001600_settlement_accounts` 已在该克隆库应用，共 41 条迁移。迁移 SHA-256：`5511aa8097afca9590a5f99ba9e0e7cf734e1560e127e54d28ccbd996cf07e1e`。

真实 HTTP 验证使用 `server/src/scripts/checkSettlementHttp.ts`，必须显式设置 `AEROLINK_SETTLEMENT_INTEGRATION=true` 并命中上述专用库。使用真实认证中间件、角色策略、结算路由、PostgreSQL 和幂等服务，由 Supertest 管理短期本地监听，不启动后台 Worker。

通过的链路和反例：

- 从实际销售/采购金额创建应收应付，拒绝伪造金额、缺少幂等键和销售写入；销售列表没有应付账户 ID，跨销售订单读取被拒绝。
- 收款 → 信用冲减 → 退款 → 退款冲销，逐步核对未付与待退款；有退款时冲销原收款被拒绝，重复冲销被拒绝，账户版本保持回滚。
- 同一命令重试不重复记账；更改内容复用命令键失败；同版本并发账期调整只成功一条；重复外部凭证失败后新附件仍未被占用。
- 应付附件按当前权限校验，上传者降为销售也不能下载；旧链接拒绝。原收款附件撤销后累计收款保持不变，账期更正仍可登记。
- 使用旧 token 在财务角色降级后重放成功过的命令，返回 403。
- 直接数据库修改/删除历史凭证、修改基础金额或只增账户版本不写记录均失败，最终版本与记录数一致。

本次 HTTP 日志：`C:/Users/jiaxi/AppData/Local/Temp/aerolink-settlement-http-20260910.log`；tag `5cefded1be18`；销售订单 `89a294c6-c72c-4e24-a056-e51459676ff1`；应收 `67b3b586-c1d3-4098-82c1-a697d55e8035`，应付 `8ea3f454-4d8f-4b34-bd5c-02fcccf6a0f3`。合成附件只含 StoredObject 元数据，本轮 HTTP 结果不能证明真实 PDF 文件已下载。

常规检查：后端 152 文件 / 859 项通过，日志 `C:/Users/jiaxi/AppData/Local/Temp/aerolink-settlement-backend-20260910.log`；新权限与凭证行为测试 10 项通过；收紧附件反例后定向复验 9 项通过。前后端 TypeScript、相关 ESLint、功能边界 7 项及模块边界 5 项通过。数据库与 Prisma datamodel 比对无差异。当前工作区测试运行器实际为 Vitest 4.1.5，此处不宣称完成新的干净依赖安装或在线审计。

OpenAPI 覆盖 359 operations / 60 mounts / 0 inventory-only，仅新增 4 个结算操作，既有 355 个操作语义未改变。契约测试 22 项、生成类型一致性及 Redocly 校验通过，保留既有 3 个停用接口无 2xx 的警告。

数据库防护脚本 `server/src/scripts/checkSettlementDatabaseGuards.ts` 实跑通过 11 个独立回滚反例：NULL/NaN 金额、空凭证、空凭证行、缺状态指纹、错误版本或操作者、跨账户冲销、超额信用冲减、超额退款和账期投影篡改。每案核对 PostgreSQL SQLSTATE `23514` 及真实错误消息；后三项确认为延迟约束拒绝。版本、记录数、金额和账期均保持原值。日志 `C:/Users/jiaxi/AppData/Local/Temp/aerolink-settlement-dbguards-20260910.log`，tag `dd457c2ee3d5`。脚本即使发现约束未拦截，也通过哨兵异常强制回滚，避免把错误夹具提交入库。

## 财务与销售页面验收（2026-09-10）

订单详情已接入结算面板。财务可建立应收/应付、登记收付款、减免、退款、全额冲销和账期调整；销售仅查看授权订单的应收摘要与凭证。金额使用十进制字符串，失败重试保留幂等键，已保存但订单刷新失败时明确提示保存成功，避免引导重复登记。

专用浏览器配置 `playwright.settlement.config.ts` 仅在显式开关和上述独立库同时满足时运行。最终 run3 使用真实认证、数据库、上传及下载接口：应收 12,000 USD → 收款 → 减免 1 USD → 退款 → 冲销退款 → 退款 0.0001 USD → 调整账期；另建立应付 7,500 USD 并登记全额付款。应收最终有效已收 11,999.9999 USD、未收 0、待退款 0.9999 USD；应付未付 0。销售响应与页面均不含该应付账户，上传后的 PDF 下载逐字节比对一致。财务录入和销售查看均检查 390px 宽度，未发现横向溢出。

- 最终浏览器结果：1/1 通过，retries=0，总耗时 39.5 秒。日志 `C:/Users/jiaxi/AppData/Local/Temp/aerolink-settlement-ui-run3-20260910.log`；tag `017a2867f2f6`；订单 `eb4e01a2-61da-4ee8-94b2-5d4172de3e78`；应收 `11cc9cc3-b19f-4630-8c0c-2d92696086d5`；应付 `bf968b29-075e-4e2e-98ac-817652f4f561`。
- 页面截图保留于 `C:/Users/jiaxi/AppData/Local/Temp/aerolink-settlement-ui-results3-20260910/settlement-ui-finance-reco-4f850-sales-sees-receivables-only-chromium/`。首次运行因测试未打开移动端导航而超时，修正测试导航后 run2/run3 通过；失败夹具和日志保留。
- 结算组件 6 项通过，覆盖权限、加载失败、切换订单、金额校验、重试、冲销及保存后的刷新失败。前端全量 27 文件 / 99 项通过，日志 `C:/Users/jiaxi/AppData/Local/Temp/aerolink-settlement-ui-frontend-tests-20260910.log`；生产构建、TypeScript、相关 ESLint 和两类边界检查通过。
- 契约测试 22 项、严格覆盖与生成类型一致性通过。移除契约中实际响应不存在的嵌套 `amounts.currency`，币种仍在账户层；保留应收响应 `purchaseCommitmentId: null`。浏览器对实际响应核对必需字段。
- 浏览器后独立库 8 个账户、累计版本 34、实际记录 34，全部账户数据库状态校验通过；日志 `C:/Users/jiaxi/AppData/Local/Temp/aerolink-settlement-ui-dbcheck-20260910.log`。测试端口 3190/5290 已停止，没有部署服务。

该财务样本使用既有已确认采购及部分交付订单，允许凭外部凭证登记预收预付；不将其描述为同一次浏览器运行完成了所有货物交付。采购、质量、直发与结算目前分别有验收证据。

## 剩余交付

1. 同一最终提交的发布验证、目标测试环境部署及真实员工岗位验收。
2. 若投入真实历史数据，先完成该批数据及关联附件的隔离恢复证据。

本增量与冻结的 M1 试用候选 `4ab3b1c61c9dd3d6997abd249884953acc15932f` 分离。D15、D16、D18 不作为首轮试用条件；D17 仅随交易补必需入口。
