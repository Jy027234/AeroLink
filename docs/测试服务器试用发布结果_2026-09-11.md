# 测试服务器试用发布结果（2026-09-11）

指定测试服务器已完成发布，可开始员工岗位试用。入口：[AeroLink 测试环境](http://124.223.196.165)。保留原有账号，未修改其密码；临时验证账号已删除。首期仍为自有库存销售、背靠背采购转售、USD、5,000/50,000 审批分级及默认禁止自批。

## 版本与远程验证

- 发布候选：`af1b1f8f4c10ca16ab35eefcd8e4de66abefff25`；[草稿 PR #2](https://github.com/Jy027234/AeroLink/pull/2) 保持未合并。
- [CI / Release Gate 34549062486](https://github.com/Jy027234/AeroLink/actions/runs/34549062486) 与 [Build and Verify 34549058848](https://github.com/Jy027234/AeroLink/actions/runs/34549058848) 均成功。
- 本次后端 152 文件 / 862 项、前端 27 文件 / 100 项通过；浏览器导航 4 项、报价合同 11 项、完整回归 60 项通过，2 个显式开关用例跳过。四层审计 exitCode 均为 0，漏洞总数均为 0。SBOM artifact ID `10180180157`，门禁 artifact ID `10180285601`。
- PR 临时合并提交 `3644da93328fc7c00d480eec5a7a72ceec2f7853` 与候选的源码树均为 `d2615ad9c4d24e33c2afe4cad0a7a3ec9e88b69d`。门禁记录全部必需作业成功，前后端生产/开发四层审计通过。
- 实际下载的 SBOM / manifest 位于本机 `C:/Users/jiaxi/AppData/Local/Temp/aerolink-release-evidence-af1b1f8-20260911`；manifest 的 SOURCE_REF / RELEASE_TAG 与候选一致，CI_RUN_URL 指向本次成功构建。CI 证据位于相邻 `aerolink-ci-evidence-af1b1f8-20260911` 目录。

## 服务器最终状态

发布目录：`/home/ubuntu/aerolink-trial-af1b1f8-20260911`。源码归档 SHA-256 为 `8326f9e89073b6a9ff6b16c8e1934b5f1687fd2a32f3f390f3f2cd4099e71a9b`，上传后校验通过。服务器构建使用既有 APT_MIRROR 参数及签名包校验，默认官方源已由 GitHub 构建验证。

| 项目 | 实际结果 |
|---|---|
| API / Worker 镜像 | `sha256:3fc48d6a21b2133001585db166d4ed1e251de180d781e5ebc6840a0e26a2554b` |
| 网页镜像 | `sha256:897d46ffb4ef26a14b03c871e1a331804bdb542cae68baace26047b382e763fb` |
| 数据库 | 41 条迁移已应用，schema up to date；本次无新迁移 |
| 服务 | API healthy；Worker running、restartCount=0；网页可从本机经公网访问 |
| 发布验证 | 镜像 ID、Worker 连续运行检查、迁移数、观测保留配置及四类对账通过 |
| 未认证访问 | `/api/auth/me` 返回 401 |

正式发布记录：`source/deploy/releases/20260911T011306Z-af1b1f8f4c10ca16ab35eefcd8e4de66abefff25.env`，`STATUS=verified`，最终 `VERIFIED_AT=2026-09-11T01:14:17Z`（北京时间 09:14:17）。发布日志、最终复验日志及无密钥的发布记录副本保存在本机 `C:/Users/jiaxi/AppData/Local/Temp/aerolink-live-release-evidence-20260911`。

数据库备份保留在该记录的 DB_BACKUP 路径，gzip 完整性检查通过，SHA-256 为 `8caa15a3300dbc081b8226383b1f7ab52b28c2cd9c495bec841111588f2ee196`。首次升级前的数据库备份、空附件归档及旧版镜像引用仍保留；未下载数据库、生产环境文件或加密密钥。镜像回滚不等同于数据库恢复。

## 发布时发现并修复的问题

此前 `681c6a5` 发布后 API 健康、对账通过，但 Worker 因 Compose 缺少 JWT 环境变量进入重启循环。原验证只核对镜像 ID，未发现这一问题。已用服务器现有变量临时恢复，再将配置修复和运行稳定性检查提交为本次候选。原发布记录保留 `worker_configuration_recovery` 状态，不将首次遗漏写成无故障发布。

Worker 已从本次候选的单一 `docker-compose.prod.yml` 强制重建，容器标签确认不再引用临时 override。API / 网页的不可变镜像与前一版相同，Compose 复用了对应容器；原 source 目录保留供已有挂载使用。最终复验在 Worker 重建后执行并通过。默认 5 秒稳定窗口仅证明启动阶段未重启，不替代长期后台任务或真实邮件发送验收。

## 实际浏览器与会话检查

随机临时只读账号完成 HTTP 登录、身份读取、能力检查、HttpOnly 刷新 Cookie、刷新及退出；退出后访问令牌和刷新 Cookie 均被拒绝（401）。镜像内 Chromium 在 1360×900 视口登录后，确实渲染“今日待处理需求”和“销售漏斗”，随后退出成功。用户/会话数检查前后均为 1 / 3，临时账号已清理，未写业务单或发送邮件。

页面截图：[工作台](C:/Users/jiaxi/AppData/Local/Temp/aerolink-live-main-page-smoke-681c6a5-20260911-1360x900.png)。这项页面证据来自相同不可变网页/API 镜像；没有因部署配置变动重复声明新增业务覆盖。只读 viewer 对 RFQ、供应商及报价的并行请求返回预期 403，工作台主内容正常显示。早一轮只拍到加载中的截图不算页面成功证据。

服务器尚无业务单据；四类对账均为零业务记录，因此不构成真实历史交易或员工全流程验收。已有带数据的合成采购、混合交付及结算验证另见 [完整业务候选验收](完整业务试用候选验收_2026-09-10.md)。

## 下一步待办

1. 管理员在“系统设置 → 用户”配置实际试用人员，按审批政策安排独立审批人；验证账号不是员工账号。
2. 员工分别走一笔自有库存销售和一笔背靠背采购样本，确认报价审批、独立质量审核、交付签收、收付款及凭证。记录具体页面、单号和预期行为，优先关闭试用阻断。
3. D17 记录两项显示问题：工作台成交额仍使用 `¥` 符号；只读 viewer 的页头角色显示为“销售员”。试用期间以交易单据的 USD 和实际能力控制为准，后续统一显示，不为此新增待办编号。
4. D15、D16、D18 保留后续优化，不作为此次首次试用新增门槛。真实历史数据仅在计划导入时补齐该批数据及附件恢复验收。
